# sungso — 프로젝트 루트

성우 & 소희의 결혼 준비 공간. GitHub Pages로 배포되는 정적 사이트 모음.

**배포 URL:** `https://CastleRain.github.io/sungso/`
**저장소:** `https://github.com/CastleRain/sungso` (branch: `master`)

---

## 폴더 구조

```
sungso/
├── index.html       ← 허브 랜딩 페이지 (이 문서의 대상)
├── css/
│   └── style.css    ← index.html 전용 스타일
├── js/
│   ├── app.js       ← 파티클, Flatpickr 초기화, D-day 초기 렌더
│   └── firebase.js  ← Firebase 모듈 (이벤트 CRUD, D-day 카드, 달력)
├── wecost/          ← 결혼 비용 관리 앱 (AGENTS.md 있음)
├── honeymoon/       ← 몰디브 신혼여행 플래너 (AGENTS.md 있음)
└── homehunt/        ← 방문 아파트·실거래 지도 포털 (AGENTS.md 있음)
```

---

## index.html 구성

빌드 도구 없음. 외부 의존성은 CDN으로만 로드. CSS/JS는 별도 파일로 분리.

### 외부 의존성
| 라이브러리 | 용도 | CDN |
|---|---|---|
| Firebase JS SDK v10.12.0 | Firestore 실시간 DB | `gstatic.com/firebasejs/10.12.0` |
| Flatpickr | 날짜 선택 달력 UI | `cdn.jsdelivr.net/npm/flatpickr` |
| Flatpickr 한국어 locale | 월/요일 한국어 표시 | `flatpickr/dist/l10n/ko.js` |

### 스크립트 구조

1. **`js/app.js`** (일반 스크립트) — Firebase 의존성 없는 코드
   - 파티클 생성 (하트/별 16개, CSS 애니메이션)
   - Flatpickr 초기화 (`#formDate`, 한국어, 오늘 이후만 선택)
   - 결혼식 D-day 초기 렌더 (Firebase 연결 전 로딩 상태용)

2. **`js/firebase.js`** (`type="module"`) — Firestore 연동 코드
   - Firebase 초기화 → Firestore `events` 컬렉션 `onSnapshot` 구독
   - `renderDdayCards()` — 핀된 이벤트를 날짜 적게 남은 순으로 카드 렌더
   - `renderList()` — 이벤트 리스트 렌더 (핀 토글, 삭제)
   - `renderCalendar()` — 달력 뷰 렌더
   - 이벤트 추가 (`addDoc`) / 삭제 (`deleteDoc`) / 핀 토글 (`updateDoc`)

### 주요 DOM ID
| ID | 역할 |
|---|---|
| `ddayRow` | D-day 카드들을 담는 flex 컨테이너 |
| `ddayNum` | 로딩 상태용 초기 D-day 숫자 (Firebase 연결 후 전체 교체됨) |
| `eventList` | 이벤트 리스트 렌더 컨테이너 |
| `calGrid` | 달력 그리드 렌더 컨테이너 |
| `calPopup` | 달력 날짜 클릭 시 팝업 |
| `addForm` | 일정 추가 폼 (토글) |
| `formTitle` / `formDate` | 추가 폼 입력 필드 |

### D-day 카드 동작
- 핀된 이벤트가 없으면: 결혼식 D-day 카드 1개 기본 표시
- 핀된 이벤트가 있으면: 핀된 이벤트만 카드로 표시 (날짜 적게 남은 순, 가로 스크롤)
- 이벤트 행 클릭 = 핀 토글 (행 전체 또는 📌 버튼); 핀 상태는 Firestore에 저장

---

## Firebase

**프로젝트:** `sungso-358cb`

```js
// js/firebase.js 에 하드코딩된 config
apiKey:            "AIzaSyBz-P5ycMAjYZBV7hkcZDrmq28EAw7Hsp8"
authDomain:        "sungso-358cb.firebaseapp.com"
projectId:         "sungso-358cb"
storageBucket:     "sungso-358cb.firebasestorage.app"
messagingSenderId: "143797950443"
appId:             "1:143797950443:web:95b0f616246d84aae3bae"
```

**Firestore 컬렉션**

`events` (sungso 루트 — 일정 관리):

| 필드 | 타입 | 예시 |
|---|---|---|
| `date` | string (YYYY-MM-DD) | `"2027-03-06"` |
| `title` | string | `"결혼식"` |
| `emoji` | string | `"💒"` |
| `pinned` | boolean | `true` (없으면 false 취급) |
| `createdAt` | serverTimestamp | — |

`resort_notes/{resortId}/comments` (honeymoon — 댓글 메모):

| 필드 | 타입 | 예시 |
|---|---|---|
| `author` | string | `"성우"` \| `"소희"` |
| `text` | string | `"라군뷰가 정말 예쁘다"` |
| `createdAt` | serverTimestamp | — |

**보안 규칙:** 현재 배포본은 `allow read, write: if true` (전체 공개). 저장소의 `firestore.rules`는 HomeHunt/서버 캐시를 차단하도록 준비됐지만, 기존 일정·재무·여행 컬렉션은 호환성을 위해 공개 상태를 유지한다. 전체 비공개화에는 Firebase Authentication이 필요하다.

**이벤트 추가/수정:** Firebase Console → Firestore → `events` 컬렉션에서 직접 편집하거나 페이지 UI를 사용.

**초기 시딩:** 컬렉션이 비어있을 때 페이지 첫 로드 시 자동으로 기본 이벤트 8개 삽입.

---

## 디자인 시스템

**색상**
- 메인 핑크: `#FF6B9D`
- 연한 핑크: `#FF85B3`, `#FFB3CC`, `#FFCCE0`, `#FFF0F5`
- 배경 그라디언트: `#FFF5F7` → `#FFF0EA`
- 텍스트: `#1a1a1a` (제목), `#333` (본문), `#aaa` (서브), `#ccc` (힌트)

**공통 패턴**
- 카드 border-radius: `22px` (프로젝트 카드), `16px` (이벤트 행), `28px` (D-day 카드)
- 그림자: `box-shadow: 0 2px 14px rgba(0,0,0,0.055)`
- hover: `translateY(-5px)` + shadow 강화 + 핑크 border

---

## 서브 프로젝트 (간략)

| 폴더 | URL | 데이터 소스 | 설명 |
|---|---|---|---|
| `wecost/` | `/sungso/wecost/` | Firebase Firestore | 결혼 비용·저축·집 계획 4탭 재무 대시보드 |
| `honeymoon/` | `/sungso/honeymoon/` | JS 하드코딩 + Firebase | 몰디브 12개 리조트 비교, 토너먼트, PDF 뷰어 |
| `homehunt/` | `/sungso/homehunt/` | localStorage + 국토부 API/JSON | 둘이 본 아파트 지도 기록, 면적별 실거래·추세 |

각 서브 프로젝트는 독립적인 `index.html` 보유. 세부 내용은 각 폴더의 `AGENTS.md` 참조.

**중요:** `wecost`는 Google Sheets CSV에서 **Firebase Firestore로 마이그레이션 완료**. `honeymoon`은 리조트 데이터는 JS 하드코딩이지만, **댓글 메모·커플 picks·일정표는 Firebase Firestore 사용** (세부 컬렉션은 `honeymoon/AGENTS.md` 참조).

---

## 작업 규칙
- 의미 있는 변경/완료 항목이 생기면, 매번 사용자에게 묻지 말고
  "진행 상황" 섹션을 알아서 업데이트할 것
- 큰 작업 끝나면 "다음에 뭐 할지" 도 한 줄 남겨둘 것

---

## 진행 상황

### 2026-09-10 — HomeHunt 4.18 거래가 활발한 단지 추천

- 거래 빈도·세대수 대비 회전율·월별 꾸준함을 추천 100점 중 5점으로 반영. 통근·주차·예산 배점과 제외 조건을 유지하고 규모 3·연식 7점으로 조정했다.
- 가격 필터 전 단지 전체의 공공 매매를 집계하고 기간·건수·최근월 신고 진행 여부를 표시. 누락·오래된 자료는 보류하며 기존 검색 보강 시 현재 후보·통근을 보존한다. 관심·계정 백업에는 공공 거래건수만 보관한다.

- 일반·역 자료 검사 1,313개와 문법·diff, Render 번들 검증 통과. 로컬 실제 UI 및 350px 카드에서 가점·미확인·최근월 신고 안내를 확인했다. 보유 공공자료 31,795건·575후보로 산술 분포를 검증했으며 오래된 자료는 실제 점수와 분리했다. 보강 중 계정 전환·취소·부분 실패와 서버 최근 검색의 원자적 교체를 검증했다. 통근 추가 호출 없음.

- `73c47b8` master Pages 배포(34383148836) 성공. 공개 UI 4.18.0의 5점 배점·계산 안내와 새 API 2.11 계약을 만족하는 온라인 서버 정상 연결을 확인했다. 계정의 기존 최근 검색 46곳 보존, 가로 넘침 없음. 사용자 원래 탭·회사 조건을 변경하지 않고 검증했으며 통근 추가 호출 없음.

**다음:** 실제 검색의 거래활동 분포와 최근월 신고 지연을 확인하며 선호 가점을 조정한다.

### 2026-09-10 — HomeHunt 4.17 예상 총가격·실제 가격 표시 개선

- 조회기간과 학습기간을 분리해 받은 최대 5년 이력을 전망에 재사용. 1년·3년 자료의 검증 부족을 설명하고 5년 자료 확장 버튼, 기간 변경 시 최근 거래 없는 선택 면적 보존을 추가했다.
- 기존 추세 기준을 유지하면서 가격 유지 방식을 자체 과거 오차·시차별 범위로 검증해 선택. 자료량·신선도·검증 사례·20% 오차 기준과 최근 2개월 제외를 유지한다. 보유한 6개 단지·109개 시계열에서 전망 제공 0→30개, 나머지 79개 보류 유지.
- 예상 평균 총가격·참고 범위·학습월/목표월을 명시하고, 보류 시에도 최근 실제 거래월·최근 3개월 총가격·건수·범위를 표시. 부분·신고 진행·오래된 자료를 구분하고 계산 방법·오차를 펼쳐 본다. 방식 선택과 범위 산정의 같은 표본 사용을 독립 적중률로 표현하지 않는다.
- 일반·역 자료 검사 1,231개와 문법·diff 통과. 실제 메가트리아 84.7㎡ 전망·1년 전환 후 같은 전망 유지, 139.7㎡ 보류 후 실제 가격 유지, 위브 1년→5년 확장 후 같은 85㎡ 전망 표시·1280px 가로 넘침과 브라우저 오류 없음을 확인. UI 4.17.0이며 API·회사 조건·통근 호출은 변경하지 않았다.

**다음:** 실제 거래가 추가될 때 전망 기준을 다시 검증하고 희소 면적의 지역 가격지수 보강 가능성을 별도로 검토한다.

### 2026-09-10 — HomeHunt 4.16.1 아파트 상세 외부 지도·부동산 연결

- 집 찾기·확인한 후보·관심/방문 기록의 공통 아파트 상세 상단에 네이버 지도·네이버 부동산 검색 버튼을 추가. 새 탭으로 열어 기존 상세와 검색 조건을 유지하며 이름·주소를 URL 인코딩하고 새 탭 안내를 제공한다.
- 지도는 주소와 단지명으로 검색하고 부동산은 단지명 검색 결과로 연결. 결과가 없던 기존 모바일 검색 주소를 공식 PC 검색 주소로 교체하고 기존 후보/방문 카드에도 적용했다. 대원칸타빌1 지도·부동산 결과와 죽전현대1차 검색어 전환을 실제 확인했다.
- 일반·역 자료 검사 1,195개와 문법·diff 통과. 로컬 1280px 상세 배치·새 탭·원래 상세 보존·브라우저 오류 없음 확인. 새 API 키나 통근 조회 없이 UI 4.16.1로 반영한다.

**다음:** 네이버의 기존 PC 서비스 종료에 맞춰 새 부동산 서비스의 검색 링크 전환을 확인한다.


### 2026-09-10 — HomeHunt 4.16 신혼 접수·검색 중 편집·후보 탭 수정

- 신혼 빠른 보기를 실제 신혼/특별공급 접수 일정으로 판정하고 기본 목록에서 마감 공고 제외. 신혼 선택을 유지한 전체·마감 보기와 카드 숫자·접수 배지를 일치시켰다. 실제 수집 1,161건으로 유효/일정 확인 5개·마감 256개 전환을 확인했다.
- 가격 조회·주소 보강 중 전체 조건과 빠른 조건을 편집할 수 있게 수정. 새 조건 적용은 이전 조회를 중단하고 명시적 재검색을 기다리며 늦은 응답·새 검색 충돌을 방어한다. 조건 변경 중 도착한 통근 사용량은 이전 조건의 내역으로 표시하고 새 후보·잔여량을 덮지 않는다.
- 지금 확인·가격 후보·지역 범위를 독립된 탭 패널로 분리해 가격·시설 안내가 다른 탭 내용을 가리지 않게 했다. 비동기 결과 반영 후 선택 탭 유지, 키보드 이동·스크롤·390px 조건창 검증.
- 월 자료 2개씩 진행 상태를 저장해 8개월 검증 작업의 부모 문서 읽기 25→13회·쓰기 10→6회. 큰 자료는 7MiB 합계 기준으로 나눠 저장하고 성공분·실행 잠금을 보존한다. 공급자 요청 수/동시성은 유지. 전체 1,195개 검사·서버 번들 통과, 검증 중 실제 통근 호출 없음.

- `65bb3ee` master Pages 배포(34369222504) 및 Render Free 자동 배포 성공. 공개 UI 4.16에서 신혼 5개/마감 256개 전환, 세 패널 분리·1280px 가로 넘침 없음·브라우저 오류 없음 확인. 배포 작업 공간에서도 1,195개 검사 통과. 개인 조건·통근 경로 추가 조회 없이 검증했다.

**다음:** 실제 회사 조건의 검색 체감 시간을 확인하고 최초 조회·재사용·위치 확인의 대기 구간을 구분해 개선한다.

### 2026-09-09 — HomeHunt 4.15 계정 검색 복원·주변 지역 우선 조회

- 로그인한 계정의 최근 가격 검색을 자동으로 찾아 복원하고, 미완료 검색은 남은 자료만 명시적으로 이어 조회한다. 같은 가격·면적·지역 조건의 24시간 이내 작업을 공유 재사용하며 최신 재조회 선택을 제공한다. 원월 자료는 24시간 작업에 두고 최근 결과 1건만 계정별 7일 압축 보관한다.
- 회사 비중과 공식 역 주소·위치를 참고한 주변 8개 시군구 조회 및 전체 지역 확장을 추가. 실제 N분 통근 가능 판정과 구분하고 위치 미확인 시 전체 조회로 돌아간다. 회사 좌표·경로는 가격 작업에 저장하지 않는다.
- 공공 주차·난방·승강기 결과를 원 만료시각 그대로 IndexedDB에 최대 3,000곳 보관. 재방문 시 한 번에 복원하고 없는 정보만 API로 조회한다. 기존 Firestore 목록 7일·시설 1일 캐시 유지.
- 가격 월 조회 동시 2개·최종 자료 읽기 동시 4개로 제한해 처리하고, WeCost 일시 오류로 후보가 지워지는 문제와 여러 탭의 검색 취소 간섭을 수정. 계정·조건 전환 뒤 늦은 응답, 보관본 동시 갱신·읽기, 무료 정리 한도를 검증했다. 일반·역 자료 1,173개 검사와 Render 번들 통과. 통근 API 호출 없음.

- `57c005a` master Pages 배포(34362519168)와 Render 무료 서버 배포 성공. 실제 공개 로그인 계정의 가격 후보 211곳을 복구하고 분양 메뉴 왕복·새로고침 자동 복원을 확인했다. 시설 211곳 처리 완료(공식 확인 111·대조 미확인 100·실패/대기 0), 재방문 캐시 복원과 1280/390px 배치·브라우저 오류 없음 검증. 로컬 API도 2.10.0으로 맞췄으며 통근 추가 호출·요금제 변경 없음.

**다음:** 공개 화면에서 회사 위치를 재확인한 뒤 주변 8개 시군구부터 탐색하고 필요할 때 전체 지역으로 넓힌다.


### 2026-09-09 — HomeHunt 4.14 분양 빠른 보기·목적지 함께 편집

- 분양 요약 카드 4개를 선택·해제 가능한 필터로 연결. 지역·검색·관심·개인 조건 안에서 숫자와 목록을 일치시키고, 새 공고는 명시적으로 확인 완료하기 전까지 유지한다. 0건 필터에는 해제 안내를 제공한다.
- 목적지 창에 임시 목록을 추가해 여러 회사의 위치·비중·시간 초과 조건을 이어 편집한 뒤 전체 저장. 취소와 변경 없는 저장은 기존 조건·현재 결과를 보존하며, 미선택 위치·늦은 검색 응답·외부 복원 충돌을 처리한다.
- 일반·역 자료 1,120개 검사와 문법·diff 통과. 로컬 실제 공고 4·9·261개 목록 일치, 장소 두 곳 연속 입력과 취소 후 회사 40·50·10 비중 유지, 1146/390/430px 화면을 검증했다. 통근 API 추가 호출 없음.

- `9adf623` master Pages 배포(34310305814) 성공. 공개 4.14.0에서 접수 중 4개 목록과 목적지 3곳의 비중·개별 초과 설정 보존, 편집창 전환·취소·브라우저 오류 없음 확인.

**다음:** 공개 화면에서 저장 회사 위치를 재확인하고 한 창에서 필요한 목적지를 함께 편집한다.

### 2026-09-09 — HomeHunt 4.13.1 Render 무료 검색 서버 공개 연결

- 사용자 선택에 따라 GitHub Pages 화면·Firebase Spark 개인 저장을 유지하고 Render 무료 Node 서버를 구성. Firebase Functions와 인증·가격 작업·통근·시설 코어를 공유하며 싱가포르·Node 22·상태 확인·필요 경로의 master 자동 배포를 설정했다.
- 공개 상태 확인은 개인정보·공급자 호출 없이 처리하고, 무료 서버 시작을 기다리는 동안 지도·기록 조작을 유지한다. 검색 재전송 없이 준비 요청을 합치며 동시 요청·대량 가격 작업을 제한한다.
- 만료 공공 캐시·검색 작업을 6시간 간격으로 소량 정리하고 당일·전일 통근 장부와 개인 기록을 보존. 일반·역 데이터 1,097개·Firestore Emulator 보안·정리 20개와 서버 번들·실제 Render Node 22 빌드 검증 완료. 전용 서버 계정·최소 역할·비밀 설정은 사용자 구체 승인 후 적용했다.
- `f269d0e` master Pages 배포(34244603656)와 같은 소스의 Render Free·싱가포르·Node 22 Live 확인. `https://sungso-homehunt-api.onrender.com/api` 무로그인 401·Google 회원 200·공개 상태 200과 공식 단지 17,851곳, 2개월 가격 작업 완료·재조회 일치를 검증했다. 공개 UI 4.13.1의 메뉴·검색 조작·1280px 무가로넘침과 단지 검색·선택 후 최근 1년 실거래 59건 표시를 확인했다. 공식 시설의 주차·지역난방·승강기 및 NAVER 장소 검색 200·5개 결과도 검증했다.
- 한국 날짜 2026-09-09 통근 전환 표식 적용 후 공식 역 경로 1회 성공. Kakao 사용 1/1,000회·잔여 999회가 Firestore와 화면에서 일치했으며 로컬 0회·미반영 증가분 0회 확인. 전날 363회는 이월하지 않았다. 로컬 통근 원호출을 0회로 유지하고 이후 통근은 공개 화면을 사용한다. 기존 개인 백업 UID·내용 유지, 복원 회사 위치 재확인 흐름 유지, Firebase Spark·요금제 변경 없음.

**다음:** 공개 화면에서 저장 회사 위치를 재확인하고 필요한 후보만 통근 확인한다. 첫 접속 약 1분 대기·공유 무료 실행시간·Firestore 사용량을 운영 중 확인한다.

### 2026-09-08 — HomeHunt 공개 수집 인증·검색 서버 준비

- 기존 공공데이터 키를 사용자 승인한 GitHub Actions 비밀 설정에 등록. 청약홈·LH·SH 모두 정상 수집해 공식 공고 1,157건을 공개 sungso에 반영했다. 실거래는 새 GitHub 실행 서버에서 9개 지역·40개월 매매·전월세 491,000건을 정상 수집했다. 추적 단지가 없어 개별 정적 이력은 0곳이며 검색 API와 구분한다.
- UI 4.12.2/API 2.9.0용 클라우드 시설 조회·공식 자료 캐시, 개인 백업 보존, 로그인 전 무호출과 재개, 부분 가격의 원수집일 보존 구현. 자동 검사 1,066개·실제 Firestore 보안 16개·번들 검증 통과.
- Firebase는 결제 비활성이고 기존 결제 계정도 모두 닫혀 있어 Blaze 연결을 사용자에게 요청했다. 클라우드 서버·운영 키 전송·통근 추가 호출은 실행하지 않았고 공개 검색 URL은 비워 둔다.
- `72aaf1f` master Pages 배포 성공과 공개 UI 4.12.2, 공식 실거래 집계·분양 1,157건 연결 표시를 실제 화면에서 확인. 기존 계정 전용 백업·온라인 통근 미배포 표시를 유지했다.

**다음:** 활성 Firebase 결제 연결 후 당일 호출량을 보존하며 검색 API를 배포·검증한다.

### 2026-09-08 — HomeHunt 4.12.1 시설 확인 반복 대기 수정

- 정상 미매칭·모호 결과의 캐시 만료 누락과, 화면 렌더마다 완료 항목을 다시 대기열에 넣던 연결을 수정. 정렬·지도·시설 반영 렌더는 완료 상태를 유지하고 새 가격 검색 완료 시 만료된 정보만 재확인한다.
- 신규 단지 자동 처리와 실패·부분 확인의 직접 재시도 유지. 만료된 응답은 시설 근거로 쓰지 않고 재시도 간격을 적용하며 부분 정보의 사실 유효기간과 오류 재시도 시점을 분리했다.
- 579곳 완료·5분/하루 만료·반복 렌더·명시 재검색 회귀 포함 전체 1,042개 검사 통과. 로컬 검색 서버 재시작 후 정상 미매칭의 공식 캐시 만료 확인, 카카오 사용 363/1,000 유지.
- 로컬 실제 가격 574곳+저장 후보의 중복 제외 579곳 완료(공식 확인 294·단지 대조 미확인 285·대기 0)와 후보/지역 화면 전환 후 완료 유지 확인. 릴리스 `bb8a825`를 최신 `master` 자료와 통합해 Pages 배포 성공. 공개 4.12.1 HTML·40개 CSS/JS·수정 모듈 5개 및 실제 지도·예산 팝업·후보·분양 메뉴 검증.

**다음:** 공개 분양 수집 인증과 가격·통근·시설 검색 서버 연결을 별도 작업으로 보강한다.

### 2026-09-08 — 공개 sungso HomeHunt 화면 확인

- 공개 sungso 홈의 HomeHunt 링크, 네이버 지도, 조건창·메뉴 전환, 공식 단지 검색·선택 및 WeCost 목표가격 표시를 실제 브라우저에서 확인했다. 1280px 화면에서 가로 넘침 없음.
- 공개 HTML은 UI 4.4.0으로, 작업 브랜치에 푸시한 4.12.0은 아직 미반영. 공개 화면의 실거래·통근 서버 미배포와 청약홈·LH 인증 실패에 따른 공고 0건을 확인했다.
- 현재 사용 중인 로컬 탭과 통근 결과를 유지했으며, 이번 확인에서 가격·통근 공급자 추가 호출이나 공개 배포는 수행하지 않았다.

**다음:** 최신 화면을 `master`에 통합하는 릴리스와 공개 수집 인증·검색 서버 연결을 각각 해결해야 로컬과 같은 기능을 제공할 수 있다.

### 2026-09-08 — HomeHunt 4.12 Git 반영 검증

- 현재 작업 브랜치 `codex/homehunt-v3-redesign`의 변경 범위를 HomeHunt, WeCost 목표가격 연결, Firebase 서버·보안 설정으로 정리. 실제 환경변수·캐시·생성된 빌드 파일과 별도 신혼여행·IDE 변경은 제외한다.
- 전체 테스트 1,023개와 역 데이터 검사 6개, 주요 JavaScript 문법·diff 검사 및 Firebase 서버 번들 빌드 통과. 가격·통근 추가 조회 없이 검증했다.

**다음:** 작업 브랜치를 원격에 반영하고, 공개 배포는 최신 `master`와 통합하는 별도 릴리스로 진행한다.

### 2026-09-08 — HomeHunt 4.12 분양 공고 선택 지도 (로컬)

- 분양 공고 카드 선택 시 상세 상단의 독립 지도에 공급 위치 표시. PC는 목록·상세 작업 영역, 모바일은 상세 지도로 이동하며 큰 집 찾기 지도와 외부 지도 검색 연결 유지.
- 주소와 좌표를 같은 공급 위치로 대조하고 단일 지도/조회 결과 재사용. 늦은 응답·화면 이탈·주소 누락·복수 공급지역·지도 실패를 처리. 개발사업 주소 검색 실패는 법정 지역으로 1회 추가 확인하며 지역 참고와 실제 단지 위치 미확인을 명시.
- 더샵 분당센트로의 제공 좌표·실제 지도, 고촌센트럴자이의 신곡리 참고 위치, 주소 없는 LH 공고 안내 및 390/430px 무가로넘침 검증. 전체1023개·문법·diff 통과, UI4.12.0/API2.9.0 로컬 적용. 기존 통근 탭 보존, 통근 추가 호출·공개배포 없음.

**다음:** 개발사업 주소·복수 공급지역의 공식 상세 위치를 보강한다.

### 2026-09-08 — HomeHunt 4.11 통근 자동 확인·공식 단지 대조 개선 (로컬)

- 사용자가 30·100·200회 중 신규 호출 한도를 선택해 시작하는 자동 통근 확인(기본 100회)과 중지를 추가. 실제 사용량·일일 잔여량을 적용하고 조건/공급자 변경·오류·영수증 누락 때 중단, 확인한 후보와 가격 유지.
- 로컬·클라우드 배치에 필수 회사 비중순 조회와 확정 탈락 후 나머지 경로 생략을 선택 적용. 미확인·시간 초과 허용만으로 탈락시키지 않으며 생략 경로와 부분 전체 평균을 만들지 않음. 조건 변경 뒤 늦게 도착한 사용량도 집계하고 새 후보에는 경로를 적용하지 않음.
- 법정동·지번·차수·세대수 근거로 제한된 명칭 차이 연결을 보강. 동일 579곳 공식 연결 249→294곳(45곳 증가), 대조 미확인 285곳·실패 0곳. 금화5단지 실제 주차 681대·지역난방·승강기17대 확인. 청명1·2 통합 자료는 개별 숫자/점수를 보류하고 이유 표시.
- UI 4.11.0/API 2.9.0 로컬 반영, 전체 테스트 997개·문법·diff 및 실제 앱/자동 확인 가상 화면 검증. 기존 탭의 통근 충족 9곳 유지·금화 시설 반영, 카카오 신규 호출 0회(잔여 637회), 공개 배포 없음.

**다음:** 새 자동 확인으로 필요한 후보를 비교하고, 남은 공식 주소 불일치와 통합 단지의 개별 시설 근거를 보강한다.

### 2026-09-08 — HomeHunt 4.10 저장 후보·전체 가격 후보 시설 자동 보강 (로컬)

- 저장한 후보와 새 가격 검색의 모든 후보를 단지별 중복 없이 자동 보강. 목록에 주차·난방·승강기를 표시하고 상세의 동수·최고층·사용승인·충전기·공용시설 연결 유지. 개인 주차값을 우선하고 정보 도착 시 점수·목록을 갱신하되 입력 중인 주차 폼은 보존.
- 진행률·확인/일부/미매칭/실패/대기 건수, 일시정지·재개·미완료 재시도 구현. 지역·통근 필터와 관계없이 전체 가격 후보를 처리하며 일부 실패해도 가격·통근 원본 유지. 권한·쿼터 오류와 연속 연결 실패 시 자동 중단.
- 서버 전체 외부 조회 동시 2개·시작 간격 200ms, 공개 목록 7일·기본/상세 1일 캐시와 동일 요청 통합. 클라이언트 유효기간과 재검색 대기열을 일치시키고 만료된 정보만 재조회.
- 저장 6곳 모두 실제 연결, 새 가격 후보 574곳과 합쳐 중복 제외 579곳 전량 확인: 공식 연결 249곳·단지 대조 미확인 330곳·조회 실패 0곳. 저장 후보 새 방문의 공개 캐시 재사용 및 주차 점수 반영 확인. 카카오 추가 호출 0회(잔여 727회 유지).
- UI 4.10.0/API 2.8.0 로컬 반영. 900곳 전량 처리·TTL·오류/교체/입력 보존 포함 전체 테스트 925개, 문법·diff 및 390/430px 카드·상세·브라우저 무오류 확인. 공개 배포 없음.

**다음:** 미매칭 단지의 공식 명칭·주소 근거를 보강하고 관리비를 대상 월·집계 범위와 함께 연결한다.


### 2026-09-08 — HomeHunt 4.9 K-apt 공식 주차·단지정보 (로컬)

- 사용자 활용신청 후 기존 공공데이터 키로 목록 V4·기본/상세 V5 실응답 확인. REB 식별자와 K-apt 코드를 분리하고 같은 법정동·명칭/공식 별칭·주소가 확인된 단지만 연결.
- 후보 상세에 주차·난방·승강기·동수·사용승인·최고층·충전기·공용시설 표시. 공식 주차를 점수·후보 목록에 반영하고 개인 확인값 우선, 누락·중복 매칭·부분 실패는 미확인 유지.
- 공개 목록 7일·기본/상세 1일 캐시와 동시 요청 통합. 서버 재시작·브라우저 새로고침 후 재사용 확인. 실제 세 단지 조회, 390/430px 상세 배치·브라우저 무오류·전체 테스트 881개·문법/diff 통과.
- UI 4.9.0/API 2.8.0 로컬 적용, 기존 브라우저 탭 자동 새로고침·Kakao 통근 추가 호출·공개 배포 없음. 상세 신청·API 버전·연결 범위는 `homehunt/docs/additional-property-data.md` 갱신.

**다음:** 다른 후보의 공식 매칭·누락 범위를 확인하고 월별 관리비를 대상 범위와 함께 연결한다.

### 2026-09-08 — HomeHunt 4.8 작은 조건 팝업·지도 상단 정리 (로컬)

- 사용자가 제공한 네이버부동산 화면의 상단 필터·가격 팝업을 직접 확인하고, HomeHunt를 아파트 매매 실거래 기준의 지역·예산·면적·세대수·연식·통근·주차 7개 버튼으로 재구성. PC 상단 약 106px, 모바일 3열로 모든 조건을 표시한다.
- 각 조건을 임시 입력값으로 편집하고 명시적으로 적용할 때만 기존 폼에 반영. 취소·Escape·바깥 클릭·필터 전환은 원본을 보존하며, 세대수/연식/면적/가격 직접 입력과 슬라이더의 상한을 분리한다.
- 통근 창에 회사별 정규화 비중·시간 초과 허용을 표시하고 기존 편집창에 연결. WeCost 전환은 독립적인 목표가격 필드 조회 뒤에 반영하며 취소·오류·원본 변경·늦은 응답의 덮어쓰기를 차단한다. 검색은 별도 명시 버튼, 통근 자동 호출 없음.
- 추가 K-apt 목록·기본정보와 선택 공용관리비 신청 링크를 `homehunt/docs/additional-property-data.md`에 정리. 현재 주차는 개인 확인값 중심이며 새 API는 아직 연결하지 않았다.
- 전체 테스트 825개·문법·diff 통과. 실제 1280/1146px와 390/430px 화면·예산 취소·연식 적용 후 원복·회사 편집·통근 지도 전환 확인. 모바일 기록 버튼의 적용 버튼 가림도 제거. 기존 탭의 통근 충족 11곳 보존, 추가 가격/통근 조회·공개 배포 없음.

**다음:** K-apt 단지 목록·기본정보 활용승인 후 주차·난방·승강기의 공식 근거를 연결한다.


### 2026-09-08 — HomeHunt 4.7 통근 충족 지도 전환 (로컬)

- 집 찾기 상단에 `가격 후보 전체 / 통근 충족만 N` 전환을 추가. 관심 저장·단지 레이어 선택과 무관하게 현재 조건으로 실제 통근을 확인한 후보를 모으고, 후보 위치에 지도를 맞춘다.
- 통근 충족 전용 지도에서는 미확인·시간 초과·분양·방문·관심 전용 핀을 제외하고 목적지는 표시 설정을 따른다. 지역·관심 목록 범위를 해제하며 전체 가격 보기로 복귀할 때 단지 레이어를 켠다.
- 전환은 가격·통근 재조회나 통과 판정 저장을 하지 않는다. 사용자 원래 탭의 현재 통근 충족 11곳을 기존 지도 조작으로 표시하고 새로고침 없이 보존했다.
- 신규 가상 경로 기반 지도 회귀 10개 포함 전체 테스트 789개·문법·diff 통과. 새 4.7.0 화면의 전환·빈 상태·PC와 390/430px 배치·브라우저 무오류 확인. API 2.7.0 유지, 통근 추가 호출·공개 배포 없음.

**다음:** 현재 확인한 후보를 비교하고 필요한 집만 추가 통근 확인한다. 새 버튼은 새 페이지 로드부터 적용되며 원래 탭의 실시간 경로 결과는 새로고침 시 재확인이 필요하다.


### 2026-09-08 — HomeHunt 4.6.1 전체 클릭 복구 (로컬)

- 새 가격 표시 함수를 이전 캐시 파일에서 찾으면서 앱 초기화가 중단된 브라우저 오류 수정. 변경한 모듈의 전이 import와 화면 버전을 함께 갱신하고 캐시·실제 ESM 연결 회귀 검사 추가.
- 현재 탭에서 조건창·후보 페이지·저장 6곳의 상세 열기 정상 작동 확인. 기존 회사·예산 유지, 새 브라우저 오류 없음, 전체 테스트 779개 통과. 가격·통근 추가 조회와 공개 배포 없음.

**다음:** 새 export가 생기는 UI 변경마다 모듈 URL 버전과 실제 브라우저 시작을 함께 확인한다.

### 2026-09-08 — HomeHunt 4.6 부분 가격 결과·조건 가독성 (로컬)

- 일부 월 실패로 시군구 전체가 사라지던 집 찾기를 수정. 정상 월과 검증된 이전 자료를 보존하며 잠정 가격·갱신 대기 월을 표시하고, 가격 미확인 단지를 별도 목록으로 제공.
- 실패한 월·지역만 이어 조회하고 기존 후보·현재 화면 통근을 유지하도록 로컬/클라우드 API를 보강. 실제 1차 4,780곳에서 잠정 가격 730곳과 가격 확인 대기 4,050곳을 복구. 94건은 실패 월×시군구 조회 수이며 단지 수와 구분.
- 조건을 읽기 전용 항목으로 줄바꿈하고 가격 후보 전체 보기를 기본으로 적용. UI 4.6.0/API 2.7.0 로컬 반영, 자동 테스트 775개·실제 검색·부분 재조회·390/430px 조건 요약 검증. 통근 추가 호출과 공개 배포 없음.

**다음:** 최신 실거래 누락분을 보강하고 필요한 후보의 통근·주차 근거를 확인한다.

### 2026-09-08 — HomeHunt 검색 버튼 오클릭 방지 (4.5.1, 로컬)

- 지도 상단 검색 버튼은 조건창만 열고, 실제 검색은 조건창 하단에서 실행하도록 분리. 같은 유효 조건의 재실행에서도 보고 있던 지역·후보·통근 결과를 유지.
- 정상·부분 실패·만료·조회 중의 상단 클릭 무초기화·무호출을 포함해 일반 테스트 737개와 로컬 버튼 흐름 검증. 사용 중인 예전 탭은 유지하고 통근 API 추가 호출·공개 배포 없이 수정.

**다음:** 확인한 후보 비교와 주차 근거 보강을 이어간다.

### 2026-09-08 — HomeHunt 4.5 확인한 후보 보관함 (로컬)

- 통근 충족·확인 완료·저장한 후보를 별도 페이지로 모으고 지도 지역 필터와 관계없이 확인·검색·정렬·관심 저장하도록 연결. 기존 확인 후보 6곳을 관심 저장하고 새 방문의 가격 복원을 검증.
- 관심 후보에 공식 가격과 사용자 검색 조건을 보관하고 조건 일치를 비교. Kakao 경로·점수·자동 통과 판정은 저장하지 않으며 Firebase 백업 형식에서도 새 가격·조건을 보존하도록 보강.
- 동일 조건의 현재 결과 재열기와 실제 호출 사용량 표시 추가. 집 1곳·회사 3곳의 실제 검증에 3회 사용, 잔여 847회 확인. 상세·관심 저장·같은 조건 재열기는 경로 추가 0회.
- 일반 테스트 730개, 문법·diff 및 390/430px 실제 앱 렌더·로컬 저장/복원/지도 이동 확인. 사용자 요청에 따라 공개 배포는 4.4.0에서 유지.

**다음:** 저장 후보의 주차 근거와 필요한 통근 경로를 확인한다.

### 2026-09-08 — HomeHunt Firebase 개인 백업·클라우드 검색 준비

- Google 로그인·회원 전용 Firestore 규칙을 실제 프로젝트에 적용하고 HomeHunt 4.4.0에 명시적 저장·복원과 버전 충돌 방어를 추가. 현재 조건의 실제 비공개 백업·로그인 유지·경로/점수 미저장 확인.
- Firebase 검색 서버를 별도 Node 22 코드베이스로 준비: 재시작 가능한 가격 검색 작업, 공식 월 자료 재사용, TMAP 8시간 캐시, 공유 일일 통근 장부·인증·호출 제한. Kakao와 NAVER 경로는 저장하지 않음.
- 실제 Firestore Emulator 보안 16개와 전체 자동 테스트·번들 검증 완료. 무료 Spark 기록 저장과 Blaze가 필요한 검색 API 배포를 구분하고, `homehunt/docs/firebase-cloud.md`에 Pages/Firebase/Vercel 방법을 기록.

- Pages 릴리스 `5978806` 배포 성공과 공개 4.4.0 화면·모듈·지도·저장 UI 확인. 일반 테스트 694개·Firestore 보안 16개 통과, 운영 무로그인 접근 403·실제 개인 백업 검증, 통근 추가 호출 없음.
- 최종 문서 `805e5b3` 배포 성공과 공개 사이트 Google 로그인·같은 Firebase 조건 불러오기까지 확인. 로컬 조건은 그대로 보존.

**다음:** 검색 서버 배포 플랫폼과 결제 설정을 확정하고 실제 온라인 경로를 검증한다.

### 2026-09-08 — HomeHunt 가격 조회 장애 복구

- 로컬 서버는 응답하지만 외부 국토부 연결은 EACCES로 막힌 실행 환경 문제를 확인하고 서버를 재실행. 기존 조건 그대로 가격 후보 959곳 복구, 통근 추가 호출 없음.
- 월·시군구 조회 실패로 가격 후보가 없을 때 조건 불일치·통근 미확인과 구분해 판단 보류를 안내. 재검색 중 이전 실패 건수와 잘못된 통근 실행 안내 제거.
- UI 4.3.6/API 2.6.3 로컬 적용, 전체 테스트 567개·문법·diff 검사 통과. 서버 재시작 검증에 실제 외부 가격 조회 성공 확인을 추가.

**다음:** 필요한 가격 후보만 통근 확인하고 공식 주차 근거를 보강한다.

### 2026-09-07 — HomeHunt 4.3.5 통근 후보 재검증

- 같은 시군구의 저렴한 후보가 최초 위치 확인 40곳을 독점하는 편향을 수정. 회사 비중의 참고거리와 동별 분산을 적용하며, 실제 통근은 확인된 단지 좌표로만 조회.
- 위치 확인 중 일괄 통근 조회와 오래된 후보 풀 사용을 차단. Kakao 도보 단계 누락을 0분으로 표시하던 오류를 미확인·점수 보류로 수정.
- 수정 전후 실제 검증에 Kakao 60회 사용. 수정 후 10곳 중 통근 시간 충족 6곳, 그중 목표가격 이내 3곳 확인. 전체 후보 최적 순위로 과장하지 않으며 개인 경로는 저장하지 않음.
- UI 4.3.5/API 2.6.3 로컬 적용·전체 테스트 562개 및 브라우저 통근 상세 검증 완료. 공개 배포는 후속 범위.

**다음:** 유력 후보의 주차 근거·출근 시간대 이동을 확인하고 필요한 지역의 위치 확인 범위를 넓힌다.

### 2026-09-07 — WeCost 목표가격 버튼 전환 (HomeHunt 4.3.4)

- 목표집가격 바로 위에 `WeCost 금액 사용 / 직접 입력` 버튼을 배치. 연동 시 금액을 읽기 전용으로 표시하고 직접 입력으로 돌아가면 마지막 수동 금액을 복원, 출처·수동 금액을 따로 저장해 새로고침에도 유지.
- Firebase 목표 필드 하나의 저장값을 대조하고 사용자가 요청한 WeCost 연동 상태로 전환. 기존 초과 허용률·회사 비중은 유지하며 가격 후보 959곳으로 재검색, 통근 API 추가 사용 없음.
- 버튼 전환·선택 표시·편집 잠금/해제·새로고침 복원·실제 1146px 화면 무가로넘침·브라우저 무오류 확인. 전체 자동 테스트 548개 및 문법·diff 검사 통과. 로컬 UI 4.3.4, API 2.6.2 유지.

**다음:** 연동된 예산 안에서 필요한 후보만 통근 확인하고 공식 주차 근거를 보강한다.

### 2026-09-07 — Kakao 단독 통근·호출 절약 (HomeHunt 4.3.3)

- 사용자 제공 REST API 키를 Git 제외 로컬 설정에 반영하고 Kakao 단독 공급자로 전환. TMAP 키가 함께 있어도 Kakao 결과만으로 통근 판정을 완료하도록 수정.
- 가격 검색은 경로 호출 없이 수행하고, 통근 확인은 회사 비중을 반영한 조회 순서에서 한 번에 최대 10곳·30개 경로만 처리. 현재 결과의 중복 확인을 막고 개별 재확인·다음 후보 조회·남은 한도 사전검사·오류 뒤 중단을 구현.
- Kakao 공식 저장 정책을 확인해 계획했던 8시간 캐시를 적용하지 않음. 진행 중인 동일 요청만 합치며 경로·파생 점수는 현재 화면에서만 표시, 저장 후보·검색 캐시·백업에서 제외. 키 변경과 서버 재시작은 일일 장부를 초기화하지 않음.
- API 2.6.2 재시작 및 UI 4.3.3 확인. 기존 회사 3곳·예산 조건의 가격 후보 1,071곳 중 10곳을 실제 확인해 통근 충족 9곳(중원 2·수지 7)·초과 1곳 검증. Kakao 정확히 30회 사용, TMAP 추가 사용 없음. 공개 코드에 새 키가 없고 1146px 화면의 가로 넘침·브라우저 오류 없음 확인.
- 전체 자동 테스트 544개·문법·diff 검사 통과. 검증 뒤 지도 안내의 충족/제외/미확인 수와 선택 공급자의 남은 호출량 표시를 함께 갱신하도록 보강.

**다음:** 현재 화면의 확인된 후보를 비교하고, 필요한 범위만 추가 통근 확인한 뒤 공식 주차 근거와 공개 API 배포를 이어간다.

### 2026-09-07 — TMAP 대체 대중교통 공급자 확인

- Kakao Maps REST의 버스·지하철 경로, 소요시간·환승·도보 응답과 무료 쿼터 조건을 공식 문서로 확인. 기존 어댑터는 있으나 공식 역 좌표 단일 실조회가 HTTP 403 · KAKAO_MAP_SERVICE_DISABLED로 실패해 지도·로컬 서비스 권한 활성화가 필요함을 확인.
- 두 공급자 키가 설정되면 선택과 무관하게 TMAP 최종 확인을 요구하는 화면 정책을 발견. Kakao 단독 완료는 코어에서 지원하므로 공급자 선택에 맞춘 화면 정책 변경이 후속 작업.
- ODsay도 대안이지만 기본 무료 30회/일과 대량 경로 분석 사전 협의 조건을 확인. 이번에는 코드·공급자 설정 변경 없이 읽기 전용 조사와 Kakao 1회 검증만 수행.

**다음:** Kakao 지도·로컬 권한을 활성화하고 Kakao 단독 검증 흐름 및 실제 회사별 경로를 확인한다.

### 2026-09-07 — 예산 확대 재조회·후보 구분 확인

- HomeHunt 예산 출처를 직접 입력으로 전환해 사용자 요청 금액으로 재검색. 기존 초과 허용률과 회사 비중·시간 초과 허용 설정을 유지하고 실제 가격 후보 증가 및 지역별 건수를 화면에서 확인.
- 통근 미확인 가격 후보 목록을 열어 가격 조건 후보와 실제 통근 확인 0곳을 구분. 공급자 오류 상태는 유지하며 경로 API 추가 호출은 실행하지 않음.
- 전체 시간 제외 체크 문구를 `시간 제한을 켠 회사`에만 적용한다는 설명으로 정리.

**다음:** TMAP 앱 키·상품 연결 정상화 후 실제 회사 경로로 후보 순위를 검증한다.

### 2026-09-07 — 통근 미확인 분리·회사별 시간 초과 허용 (4.3.2)

- 실제 사용자 검색의 가격 후보 959곳이 전부 경로 미확인임을 확인. 기본 목록·지도는 검증된 통근 조건 충족 후보만 표시하고, 미확인 가격 후보는 별도 보기로 분리. 주차·통근·역의 미확인 점수는 숫자 0 대신 점수 보류로 표시.
- 회사별 `이 시간을 넘으면 후보에서 제외` 설정 추가. 끄면 시간 초과만으로 제외하지 않고 입력 비중대로 실제 통근 부담에 반영. 사용자가 요청한 낮은 비중 회사의 초과 허용을 저장하고 다른 두 회사의 비중·시간 설정은 유지, 새로고침 복원 확인.
- 40%×20분 + 50%×20분 + 10%×90분 = 27분인 가상 후보가 60/60/10분의 55분 후보보다 우선함을 실제 앱 정렬 함수로 검증. 초과 허용 회사도 양수 비중이면 실제 경로가 필요하며 누락은 보류·확정 경로 없음은 제외.
- 통근 확인 버튼을 결과 상단에 노출하고 조회 실패를 시각과 함께 유지. 기본 목록이 비어도 별도의 대기 후보에서 검증하며, 실패·충족 0건 후 전체 목록으로 자동 전환하지 않음. Kakao 1곳의 회사 행렬이 실패하면 나머지 광역 호출 중단.
- 저장 경로의 출발 좌표 지문·대중교통 기본 8시간/자동차 20분 유효시간·명시적 만료를 적용. 선별 결과·점수 캐시도 만료에 맞춰 갱신하고, 이전 optional 경로 판정을 목적지 지문 v3로 무효화.
- API 2.6.1 재시작 후 공식 역 좌표로 TMAP 1회 조회: HTTP 403 · INVALID_API_KEY · gw. 오늘 사용량 1/10, 추가 호출 없음. 발급 앱·연결 상품 확인이 필요하며 상품 미구매를 확정한 것은 아님. 원문·비밀키는 출력·기록하지 않음.
- 전체 자동 테스트 507개와 문법·diff 검사 통과. 1146px 실제 화면에서 959개 미확인/기본 충족 0개, 평택 후보 점수 보류, 회사별 설정 저장·복원·무가로넘침·콘솔 오류 없음 검증. UI 4.3.2/API 2.6.1은 로컬 적용, 공개 배포는 미진행.

**다음:** TMAP 앱 키·상품 연결을 정상화해 실제 회사 경로 점수를 검증하고, 공식 K-apt 주차정보 연결을 진행한다.

### 2026-09-07 — WeCost 목표집가격 Firebase 직접 연결 (HomeHunt 4.3.1)

- 사용자 요청에 따라 HomeHunt에서 `wecost_settings/main.targetHousePrice`를 직접 읽도록 변경. Firestore GET 필드 마스크로 목표가격만 수신하고 기존 브라우저 전달 방식은 변경 시 재조회 신호로 사용.
- WeCost 출처 선택·가져오기·새로고침·탭 복귀 시 Firebase 값을 확인하며, 수동 입력은 자동 값으로 덮어쓰지 않음. 오류·권한 거부·시간 초과·취소 때 이전 금액으로 검색하지 않고, 중복 요청과 늦은 응답을 차단.
- 사용자 제공 접근 코드로 WeCost 집 시뮬에 들어가 저장 목표값을 대조하고, HomeHunt 직접 연결·허용률 상한·WeCost를 닫은 뒤 재조회·수동 전환/복귀를 실화면에서 확인. 목표값 원본과 다른 재무 자료는 수정하지 않음.
- 기존 접근 코드로 인한 WeCost 검증 미완료는 해소. 로컬 화면 4.3.1, API 2.6.0이며 이번 변경의 공개 배포와 TMAP 403 해결은 별도 후속 작업.
- 전체 자동 테스트 467개 통과. 실제 서비스를 이용한 동일 금액 재확인 시 후보 보존·조회 중 새 검색 차단·통근 검증 중 복귀 갱신 보류도 검증.

**다음:** TMAP 사용 권한을 해결하고 모바일 화면 및 공개 추천 API 배포를 검증한다.

### 2026-09-06 — HomeHunt 4.3 회사 비중·주차·목표가격 추천

- 회사별 입력 비중을 그대로 저장하고 합계 100%로 환산해 실제 통근 부담에 반영. 목적지 4곳 제한을 제거하고 미등록 시 공식 강남역 100%를 적용하며, 작은 비중의 회사도 개별 허용시간을 모두 확인.
- 통근 55·역 10·규모 5·연식 10·주차 10·예산 10점으로 개편. 조회시간·환승·도보·버스 부담으로 경로를 비교하고 실제 경로가 없으면 총점을 보류. 확인된 시간 초과·주차 불가·가격 상한 초과는 기본 목록에서 제외.
- 목표집가격 직접 입력·초과 허용률과 WeCost의 같은 브라우저 목표금액 연결을 추가. 재무 원본 추가 조회 없이 목표값만 전달하고, 주차는 출처를 구분하는 개인 확인값 입력으로 보강.
- 비중/선호 변경 시 기존 경로 재평가, 회사 좌표/수단 변경 시 무효화, 오래된 비동기 응답 차단을 구현. 452개 자동 테스트와 문법 검사, 실제 가격 후보 10곳·10/50/40 입력/새로고침·주차 제외/복원·데스크톱 화면을 검증.
- 로컬 UI 4.3.0/API 2.6.0이며 이번 변경은 미배포. TMAP 실요청 1회가 HTTP 403으로 실패해 실제 통근 총점 검증은 미완료. WeCost 접근 코드 화면으로 저장 목표값 대조와 모바일 화면 검증도 미완료이며 계산·연동 계약은 자동 테스트로 확인.

**다음:** TMAP 사용 권한과 WeCost 접근을 확인해 실제 출근 경로·목표값을 대조하고, 공식 주차정보 연결 및 공개 추천 API 배포를 이어간다.

### 2026-09-06 — GitHub Pages 4.2.1 배포 완료

- GitHub Pages가 `master / (root)`를 사용하고 저장소 배포 권한이 있음을 확인. 최신 원격 공급 데이터는 유지하고 HomeHunt·공용 계산·WeCost 집 탭 연결만 릴리스 체크아웃으로 분리.
- `docs/deployment.md`에 실제 Pages 배포 절차, 공개 화면/로컬 검색 서버의 기능 차이, 데이터 수집 Secrets와 전체 온라인 검색의 서버·인증 후속 순서를 기록.
- 개인 `.env`·로컬 캐시·신혼여행 수정·IDE 설정을 배포 대상에서 제외하고, 미수집 공개 실거래를 연결 완료로 표시하지 않도록 준비 상태를 보강.
- 릴리스 `0346b74`를 master에 정상 push하고 Pages 빌드·배포 성공 확인. 공개 주소에서 4.2.1 HTML·공통 지도 핀·역 JSON·공용 계산 모듈 HTTP 200, 네이버 지도·단지 클릭·390px 무가로넘침·브라우저 무오류 검증.
- 자동 갱신 분양 파일을 초기 빈 파일로 고정하던 테스트를 스키마 검사로 수정하고 첫 수집 알림 억제는 독립 fixture로 유지. 최신 master 데이터와 전체 자동 테스트 367개 통과. 새 실거래 후보·회사 검색·통근은 여전히 로컬 서버 전용임을 배포 문서와 사용자에게 명시.

**다음:** 공개 추천·통근 HTTPS API와 인증을 구현하고 공급원 Secrets를 연결해 휴대폰에서 전체 검색을 검증한다.

### 2026-09-06 (HomeHunt 4.2.1 지도 핀 통일)

- 단지 이름·조건 확인 전 표시와 분양 공고 이름을 추가하고 지역 숫자 핀과 동일한 흰 배경·둥근 외곽선으로 통일. 예전 가격 핀의 이중 배경·꼬리 제거.
- 넓은 지도 분양 이름 축약·확대/선택 시 펼침, 키보드 상세 열기와 핀 간 선택 잔상 해제 지원.
- 자동 테스트 361개, 데스크톱·390/430px 실화면·무가로넘침·브라우저 무오류 확인.

**다음:** 기존 통근 리뷰의 세 오류와 관심 지역 좌표 보강을 이어간다.

### 2026-09-06 (HomeHunt 4.2 지역 분포·입지 추천)

- 집 찾기의 기본 정렬을 회사 위치 없이 계산하는 강남·역 접근성 중심 입지 점수로 변경하고 항목별 추천 이유와 정렬 비중 전환 추가.
- 993곳을 36개 시군구의 숫자 지도·전체 지역 목록으로 나누고, 클릭 확대·지역 선택·후보 필터를 연결. 지역 대표점과 단지 위치를 구분하며 상위 40곳부터 확인하고 20곳씩 추가 보강.
- 국가철도공단 공식 역 좌표 1,090행과 강남역 기준점을 도입하고 직선거리·GTX 미수록·자료 기준일 한계 명시. 실제 지역 합계·필터·추가 위치 확인과 자동 테스트 355개와 데스크톱·390/430px 실화면·브라우저 무오류 확인.

**다음:** 기존 회사 통근 리뷰의 세 오류와 관심 지역 좌표 커버리지를 보강한다.

### 2026-09-06 (HomeHunt 슬라이더 조정)

- 집 찾기 슬라이더를 1,000세대·30년·50평·20억원·출근 120분까지로 줄이고 범위 양 끝값을 표시.
- 세대수·연식·면적·가격은 직접 입력한 큰 값을 조건·저장에 그대로 사용하도록 상한을 분리. 새로고침 유지·슬라이더 조작·모바일 화면과 자동 테스트 307개 확인, 사용자 기존 조건 복원.

**다음:** 통근 추천 리뷰의 우선 세 오류를 수정한다.

### 2026-09-06 (HomeHunt 4.1 총가격·평수별 비교)

- 실거래 그래프를 평균 총 거래가격 기준으로 전환하고 최근 거래월 평균과 선택기간 평균을 금액·거래건수와 함께 구분 표시.
- 모든 정확 전용면적의 평균가격·거래건수를 카드로 비교하고 클릭해 상세 흐름으로 이동하도록 구현. 전망 총액 표시와 실제·전망 월 정렬도 보강.
- 실제 단지 13개 면적·매매/전세·1/5년 전환, 데스크톱·390/430px 화면과 브라우저 무오류, 자동 테스트 307개를 검증.

**다음:** 통근 추천 리뷰의 우선 세 오류를 수정한다.

### 2026-09-06 (HomeHunt 조건 수정 접근성)

- 지역·세대수·연식을 기본 설정 상단으로 꺼내고 지도 조건 칩→해당 입력 바로가기를 연결. 모바일 `조건 변경` 표시·본문 스크롤·연식 0년 복원을 보강.
- 조건 변경·새로고침 유지와 390/430px 실제 화면, 자동 테스트 291개를 확인하고 화면 버전을 4.0.2로 갱신.

**다음:** 통근 추천 리뷰의 우선 세 오류를 수정한다.

### 2026-09-06 (HomeHunt 통근 추천 검토)

- 회사 위치 기반 추천을 읽기 전용 검토하고 관련 테스트 49개를 확인. 회사 수정 후 옛 기준 유지·주당 방문일 가중치 미갱신·대체 교통수단 실패 시 확정 제외 오류를 재현해 `homehunt/docs/commute-logic-review.md`에 기록.

**다음:** 세 우선 오류를 수정하고 조건 변경·부분 경로 실패의 통합 회귀 검증을 추가.

### 2026-09-06 (HomeHunt 4.0 리빌딩)

- 지도 탐색을 `지금 확인 / 가격 후보 / 지역 범위`로 재구성하고, 관심·방문·분양의 근거 상세와 3곳 판단 보드를 추가.
- 고정 72% 실거래 진행표시를 단계·경과시간·취소·재시도로 교체하고, 12개월 우선→3/5년 확장과 실패 시 자료 보존 구현.
- WeCost 계산을 공용 모듈로 분리하고 공개 금융 자동조회 없이 읽기 전용 연결 상태·개인 자금 시나리오를 추가.
- 분양 공급원 7상태·공식 신청 준비·포털 링크 전용 정책을 구현. 실제 5년 실거래 3,653건과 조건 후보 245개를 확인.
- 자동 테스트 291개와 390/430/1024/1440px 여섯 화면·실거래 필터 연동·후보 비교·자금 계산을 검증하고, `homehunt/docs/rebuild-implementation.md`에 실제 연결과 미연결 항목·실행/배포 설정을 정리.

**다음:** 운영 API 활용권한·Kakao 활성화와 Firebase Authentication 및 household 재무 권한을 연결.

### 2026-09-04 (HomeHunt 2.5 지도 중심 통합 탐색)

- `homehunt/docs/homehunt-2.5-handoff/`에 UI·화면, 데이터·로직, 다음 고도화 브리프와 실제 로컬 화면 캡처 14장을 묶어 후속 작업의 기준선을 기록하고, 내장 안내와 README의 예전 메뉴명을 현재 6개 정보구조로 통일.
- HomeHunt 메뉴를 `집 찾기 / 내 기록 / 실거래 / 분양·청약 / 사용 안내 / 연결 상태`로 재구성하고 모바일은 핵심 4메뉴와 상단 보조 진입을 유지.
- 집 찾기 지도에 실제 조건 후보 클러스터와 단지·분양·방문·관심·목적지 레이어를 추가하고, 방문 지도·목록·비교는 `내 기록` 내부로 통합.
- 후보·방문·분양에서 단지 실거래와 주변 지도까지 이어지는 교차 이동을 연결하고 분양 빠른 메뉴와 고밀도 화면을 적용.
- 검색 전 공식 단지는 가격 판정 전 기준점 12개로 제한하고 실제 검색 후 국토부 실거래 조건을 통과한 후보만 가격 결과로 표시.
- 화면·로컬 API 2.5.0 계약과 전체 자동 테스트 222개를 통과.

**다음:** 인수인계 브리프의 P0 순서대로 공식 분양 기준선, 단지 좌표 커버리지, 필터 경계 계약과 390px·430px 실화면을 검증.

### 2026-09-04 (HomeHunt 2.4 · 사용 안내·청약 준비도·휴대폰 알림)

- 처음 방문자가 목적지 등록→집 찾기→현장 기록→실거래·분양 확인으로 시작할 수 있는 독립 `사용 안내` 화면과 6개 기능 바로가기 추가.
- 서울·경기 분양/신혼희망타운을 동네·가격·평형·공급세대와 미공개 포함 여부로 거르는 브라우저 `내 조건`을 구현하고 알림 조건과 함께 로컬 저장.
- 성우·소희의 자가입력 민영 기본 84점과 신혼 공급 준비 항목을 브라우저에만 저장하며, 신혼특공·희망타운 공급비율을 당첨확률로 만들지 않고 공고일 원문 확인을 강제.
- 비공개 Telegram 그룹과 GitHub Actions를 연결해 닫힌 사이트에서도 신규·중요 변경을 받고, Repository Variables로 모바일 알림에도 지역·신혼·가격·평형·세대수 조건을 적용.
- baseline·stale·마감·삭제·비중요 변경 차단, 공식 링크 allowlist, 메시지 상한·429 재시도·해시 중복 억제를 적용하고 전체 자동 테스트 217개와 2.4.0 로컬 API·브라우저 무오류 검증 완료.

**다음:** 청약홈·LH 활용신청을 승인하고 Telegram Secrets/Variables를 설정해 실기기 알림을 검증한 뒤 Firebase Authentication·FCM 전환 여부를 결정.

### 2026-09-04 (HomeHunt 2.3 · 서울·경기 분양 알림)

- HomeHunt에 `분양 알림` 여섯 번째 화면을 추가하고 청약홈 APT·잔여·임의공급, LH 분양주택·신혼희망타운, SH 자체 분양 공고를 서울·경기로 통합.
- 검색·지역·접수상태·신혼/분양유형·관심 필터, 공고/접수/발표/계약 일정, ㎡·평 주택형·분양가·공급세대·원문 링크를 피드/상세 화면에 구현.
- 신혼희망타운 코드나 신혼부부 배정세대처럼 공식 구조화 근거가 있는 경우만 신혼 대상으로 확정하고 미제공 가격·일정·자격은 추측하지 않도록 처리.
- 공급원별 기준선·부분실패 보존·원자적 파일 갱신과 GitHub Actions 하루 3회 수집·Pages 명시 재빌드, 로컬 3시간 캐시·실패 후 15분 재시도 대기를 구축.
- 관심·읽음·알림 조건을 로컬 저장하고 신규·변경·D-3/D-1 브라우저 알림 및 캘린더 저장을 추가; 닫힌 브라우저 푸시는 Firebase 인증/FCM 후속 범위로 명시.
- SH 공식 RSS를 키 없이 수집해 EUC-KR 파싱·공급원별 기준선·부분 실패 보존을 적용하고, GH는 실시간 공식 피드가 없고 `robots.txt`가 자동 수집을 차단해 공고판·공식 문자알림을 보완 경로로 분리.
- 현재 공공데이터 키의 청약홈 401·LH 403을 실확인해 별도 활용신청 안내를 추가하고, 샘플·실제 미승인 상태와 SH 실응답을 검증한 뒤 전체 자동 테스트 189개·JS 문법 검사를 통과.
- 390px 정적 반응형 감사로 하단 6개 내비·필터·알림 모달의 무가로넘침을 확인하고 모바일 공고 선택 시 상세 카드로 자동 이동하도록 보강.

**다음:** 청약홈·LH 활용신청 승인 후 첫 공식 수집 기준선을 만들고, Firebase Authentication·Cloud Messaging으로 성우·소희 전용 백그라운드 푸시를 연결.

### 2026-09-04 (HomeHunt 2.2 지도 우선 탐색)

- 방문 지도와 맞춤 찾기를 네이버 부동산형 `전체 지도 + 한 줄 조건 칩 + 오버레이 드로어/결과 시트`로 전환해 검색·필터가 지도를 밀어내지 않도록 정리.
- 맞춤 지도를 검색 전부터 표시하고 조건 숫자 입력과 슬라이더를 동기화했으며, 결과 통계·후보 목록은 데스크톱 우측 패널/모바일 하단 시트로 분리.
- 추천 후보 묶음을 상태별 원형 숫자 클러스터로 표시하고 확대→세부 묶음→가격 핀→정확한 결과 카드 흐름을 연결.
- 2.2.0 로컬 서버에서 현재 저장 조건의 공식 가격·기본조건 후보 185개를 실제 조회하고, 데스크톱·390px 지도/패널·무가로넘침·브라우저 무오류와 자동 테스트 132개를 검증.

**다음:** 회사 A–D 실제 위치와 Kakao 선별을 연결해 제한된 TMAP 호출은 최종 후보 검증에만 사용하고, K-apt 역·주차·시설 정보를 결과 시트에 추가.

### 2026-09-04 (HomeHunt 2.1 · 평균가/검색 반응/화이트 UI)

- 사용자 화면의 실거래 대표값을 산술평균 거래가격과 평균 평당가격으로 통일하고 `1평≈3.3㎡` 의미·표본 수·최근 개별 거래를 함께 표시.
- 검색한 단지·거래유형·정확 전용면적·기간을 요약, 차트, 면적 비교, 최근 거래, 6개월 참고 예측에 하나의 데이터 문맥으로 연결.
- 검색 중 단계형 진행 상태와 스켈레톤을 추가하고, 긴 실거래 화면을 `단지 요약`·`가격 흐름`·`예측·근거` 하위 메뉴로 분리.
- 사이트 바탕을 흰색으로 바꾸고 중립 표면을 적용해 초록색은 주요 액션과 선택 상태에만 남김.
- 예전 저장 후보의 과거 대표값을 평균으로 잘못 표시하지 않고 재검색 갱신 대상으로 분리.
- 래미안안양메가트리아 실제 조회로 두 건의 산술평균, 매매↔전세·면적·1/3/5년 연동을 확인하고 자동 테스트 132개, 5개 화면 흰 배경, 데스크톱·390px 무가로넘침, 브라우저 무오류 통과.

**다음:** K-apt 상세·한국부동산원 지역 가격지수를 연결하고 인증된 Firebase에 두 사람의 방문 기록 공유를 이전.

### 2026-09-04 (HomeHunt 2.0 완료 · 다중 회사 균형 통근/안전 예측)

- 한 곳뿐이던 회사 위치를 최대 4개 목적지 A~D로 확장하고, 주당 방문일·교통수단·허용시간을 반영해 모든 필수 목적지가 확인된 후보만 통근 충족으로 판정.
- 직선거리는 후보 선별용으로만 남기고, 가장 불리한 통근을 우선 최소화한 뒤 가중 평균·도보·환승 부담을 보는 균형 점수와 목적지별 경로 행렬을 추가.
- 가격 후보 찾기와 경로 검증을 분리해 자동 TMAP 소진을 없애고, KST 일일 사용량·남은 쿼터·예상 호출 수를 확인한 뒤 상위 후보 또는 개별 후보만 정밀 검증하도록 변경.
- Kakao 대중교통 REST를 넓은 후보 선별에 쓰고 TMAP을 동일 출근시각의 최종 검증에 쓰는 공급자 선택, 배치 API와 일 10건 사전 차단을 로컬 서버에 구현.
- Kakao 1차 결과와 TMAP 최종 판정을 분리하고 Kakao·TMAP 모두 KST 일일 장부(기본 1,000/10회), 캐시 miss만 차감, 실행 전 전체 배치 한도 검사를 적용.
- 국토부 지역·월 응답이 재시도 뒤에도 불완전하면 해당 시군구 후보 전체를 추천에서 제외해 오래된 일부 거래를 `가격 검증됨`으로 오인하지 않도록 보강.
- 추천을 첫 화면으로 올리고 목적지 A~D 핀, 혼합 상태 클러스터, 예산 후보·통근 확인·최적 균형·최악 통근·남은 호출을 한 화면에서 판단하도록 UI를 재구성.
- 연결되지 않은 역 접근성은 하드 필터에서 제외하고, 방문 호가와 방문 당시·현재 동일면적 실거래 변화를 분리하며 현재 기준은 방문 후 거래만 사용하고 희소 표본은 낮은 신뢰도로 표시.
- 현재·직전 불완전월 제외, horizon별 rolling-origin, 무변화 기준 대비 5% 개선, MAE·MASE·경험적 참고범위로 예측 공개 조건을 강화.
- 단지 이력을 기본 5년으로 확장해 6개월 예측 검증 표본을 확보하고, 저장 후보의 목적지 조건 fingerprint 만료·재검증 동기화와 혼동 가능한 가짜 방문 핀 제거를 완료.
- 실제 `.env` 연결로 국토부 846건, NAVER 회사 검색·자동차 경로, TMAP 경로·무소모 한도 차단을 확인하고 자동 테스트 128개, 5개 화면·390px 무가로넘침·브라우저 무오류를 검증.
- TMAP 무료량 우회 금지, Kakao·TMAP 종량제 선택, 다중 통근·가격 구성·예측 한계를 공식 근거와 함께 `homehunt/docs/report-source.md`에 정리.

**다음:** `KAKAO_REST_API_KEY`를 로컬 환경에 연결해 실제 회사 3~4곳의 Kakao 선별→TMAP 최종 검증을 반복 확인하고, K-apt 역·주차·시설 보강 후 인증된 Firebase 저장소로 이전한다.

### 2026-09-04 (HomeHunt 1.8.0 · 로컬 키 자동 연결/모든 건물명 검색)

- 회사 위치 검색을 NAVER API HUB 지역 검색, NAVER Maps 주소, 키 없는 Kakao 공식 주소 DB 건물명 검색의 통합 UI로 확장.
- `KT`가 `정자KTe-편한세상` 아파트에 부분 일치하던 오인을 차단하고 회사 위치 검색에서 아파트 카탈로그 fallback을 제거.
- 장소 키가 없어도 등록 건물명·법인명·도로명·지번을 모달 안에서 선택한 뒤 NAVER 지도 좌표로 다시 확인하도록 구현.
- NAVER API HUB Client ID/Secret은 Git 제외 `homehunt/.env`로 재시작 후 자동 연결하거나 화면에서 현재 서버 메모리에만 연결하도록 분리하고, 공개 코드·Git·브라우저 저장소·응답에는 남기지 않도록 구성.
- 장소 검색 키 미연결·구형 서버·검색 실패를 구분해 건물 주소찾기, 입점 상호 API 연결, 지도 직접 선택을 유지.
- `KT` 공식 주소 후보 20페이지와 `푸르지오시티3차` 단일 건물 결과를 실제 브라우저에서 확인하고, 선택 주소가 NAVER 지도 좌표로 확정되는 흐름까지 검증.
- 우편번호 하위 모달의 비동기 세션 격리·포커스 이동/복귀·배경 `inert`·실패 시 지도 복구를 보강하고 390px/946px에서 가로 넘침과 브라우저 오류가 없음을 확인.
- `homehunt/.env` 자동 로딩, 공개 `.env.example`, 키별 공식 발급 가이드와 프로세스 환경변수 우선·보안 입력 fallback을 추가.
- 화면 입력은 계속 메모리 전용으로 유지하고 `.env`/환경변수 자동 연결 여부를 연결 상태에 구분 표시.
- 1.8.0 서버의 `.env` 국토부 키 자동 인식을 확인하고, 2026-08 안양시 만안구 매매·전월세 공식 API가 모두 HTTP 200·결과코드 000을 반환하는지 실연결 검증.
- `.env`의 NAVER API HUB·NAVER Directions 5·TMAP 키 실연결을 확인하고 `KT 분당본사` 5개 장소 후보, 테스트 경로의 자동차 23분·대중교통 44분 정상 응답을 검증.
- NAVER 지역 검색이 WGS84 좌표를 1천만 배 정수 문자열로 반환하는 실응답 형식을 정규화하고, 회사 결과 선택과 지도 핀까지 브라우저에서 확인.
- HomeHunt 1.8.0 공급자·검색·지도 회귀 자동 테스트 89개 통과.

**다음:** 필요한 NAVER Maps Secret·NAVER API HUB 지역 검색 키·TMAP appKey를 `homehunt/.env`에 연결해 실제 회사 검색·자동차·대중교통 경로를 검증하고, Firebase Function 배포를 이어간다.

### 2026-09-03 (HomeHunt 로컬 실거래·조건 추천)

- 서울·경기 17,851개 공식 아파트로 범위를 정리하고, 공개 코드·브라우저에 키를 남기지 않는 localhost 국토부 실거래 서버 구축.
- 자연어/직접 입력 조건에서 세대수·연식·전용면적·실거래 예산을 실제 판정하는 `맞춤 찾기` 화면 추가.
- 국토부 호출 속도 제한·재시도·오류 XML 검증·파일 캐시를 적용하고 204개 월·지역 요청이 누락 없이 완료되는지 검증.
- 관심 후보 저장·다시 보기, 지도·12개월 실거래 연결과 공식 카탈로그 기준일·미확인 교통 조건 표시를 보강.
- Tabler UI 1.4.0·Tabler Icons 3.46.0 기반 디자인 레이어를 추가해 5개 화면, 동적 카드, 폼, 모달, 모바일 내비를 부동산 서비스 톤으로 통일.
- 네이티브 폼·검색·저장 계약은 유지하고 접근성 상태, 터치 크기, 모바일 고정 액션과 반응형 레이아웃을 보강.
- 맞춤 찾기의 핵심 조건을 한 줄에 모으고 지도·후보 목록을 데스크톱 한 화면에 배치, 회사 핀·가격 핀·후보 수 클러스터를 추가.
- 회사 주소를 좌표로 확정한 뒤 전체 가격 후보를 거리순으로 재정렬하고, TMAP 대중교통·NAVER 자동차 실제 경로가 없으면 `1시간 충족`으로 판정하지 않도록 변경.
- 단지 이력을 최근 1·3·5년으로 확장하고 가격을 억·만원, 전용면적을 ㎡·평으로 통일.
- 추세 예측에 데이터 품질 게이트와 시간 순서 롤링 백테스트를 추가해 오차가 큰 결과는 보류하고 근거·한계를 화면과 조사 보고서에 표시.
- 구형 로컬 서버와 새 화면의 버전 차이를 감지해 잘린 추천 결과를 차단하고 재시작 안내를 제공.
- 이력의 서울 기준 시작·종료 월을 캐시 계약에 포함하고 부분 응답의 완전 캐시 덮어쓰기와 불완전 예측을 차단.
- 946px 핵심 조건 한 행, 결과 지도 자동 이동, 390px 전 화면 무가로넘침과 공용 모달 포커스 복귀를 검증하고 자동 테스트 75개 통과.
- 통근 조건 검색의 기본 결과·지도를 실제 경로상 시간 충족 후보로 한정하고, 미확인·초과 후보는 별도 결과 범위에서만 열어보도록 구분.
- 600개 초과의 넓은 가격 후보는 통근 미검증 상태로 기본 추천에 노출하지 않고 조건 축소를 안내하도록 차단.
- 새 검색 사전검사 실패 시 이전 조건의 결과·지도·상태를 즉시 비워 오래된 후보가 새 검색 결과처럼 보이지 않도록 보강.
- 회사 위치를 네이버 주소 일부 검색 후보 또는 전용 지도 클릭으로 좌표 확정할 수 있게 하고, 공식 주소가 입주 회사명과 다를 수 있음을 안내하며 검색·지도 응답 경합을 차단.
- 숫자 입력의 포인터 스피너와 포커스 중 휠 값 변경을 막아 작은 필터 칸의 실수 입력을 줄임.

### 2026-09-02 (HomeHunt 신규 구축)

- **부동산 포털 UI:** 네이버 지도 중심의 필터–지도–결과 3단 레이아웃, 방문 기록·실거래·연결 상태 화면 구축.
- **네이버 Maps:** Dynamic Map, 주소 검색·역지오코딩, 방문 마커, 지도 직접 선택 연동. Client Secret은 사용하지 않음.
- **방문 기록:** 매매·전세·월세, 면적·층·가격, 둘의 현장 의견·상태를 CRUD. 공개 Firestore 대신 로컬 저장 + JSON 백업·복원.
- **실거래 분석:** 국토부 매매·전월세를 5개 전용면적대로 분리, 중위 가격·3.3㎡ 단가·거래량·지역 순위·6개월 통계적 추세 예측 구현.
- **검색 캐시:** 트래킹 단지 정적 JSON, 브라우저 IndexedDB, Firebase Function의 월 원자료·단지 캐시, GitHub Actions 일일 지역 집계 구축.
- **전국·정확 검색:** 공식 법정동 269개 시군구 선택, 동명 단지 후보 분리, 실제 전용면적 0.1㎡별 이력, IndexedDB 재사용 구현.
- **공식 전국 카탈로그:** 한국부동산원 공동주택 45,920개를 정적 검색 목록으로 생성하고 지역 선택 없는 단지명·주소·동네 검색, `레미안 → 래미안` 보정, 유사 단지 추천 구현.
- **검색 표면 통일:** 지도·방문 기록·실거래·집 기록 폼을 자유 검색으로 통일하고 검색 위치 핀, 비동기 경합 차단, 모바일 필터 서랍과 공식 목록 연결 상태 추가.
- **수집 안정성:** 국토부 페이지네이션·월 원자료 캐시·부분 실패 대응·서버 서명과 호출 한도, Node 20 및 서버 컬렉션 차단 규칙 반영.
- **홈 연결:** sungso 랜딩에 HomeHunt 카드 추가, 모바일 하단 내비·JSON 데이터 파이프라인·자동 테스트 추가.
- **후보 결정 UX:** 최대 3곳 비교함, 확인 가격·면적·교통·연식·장단점·메모의 사실 기반 비교표와 로컬 선택 저장 추가.
- **방문→실거래 연결:** 방문 카드에서 공식 시군구·거래유형·가장 가까운 실제 전용면적을 자동 선택하고 현장 확인가와 최근 3건 중위 실거래 차이 표시.
- **가독성·검증:** 부동산 포털 정보 글자·모바일 비교 트레이를 개선하고 후보 비교·전국 단지 검색 순수 로직을 포함한 자동 테스트 33개 통과. 4개 화면과 390px 모바일 브라우저 검증 완료.
- **실연결 검증:** 네이버 지도 타일·방문 마커·주소 검색과 국토부 매매·전월세 API의 실제 XML 응답을 확인. 인증키 값은 저장소에 기록하지 않음.
- **단지 가격 상태 UX:** 미배포 Firebase Function의 404/CORS를 사용자 네트워크 오류로 표시하지 않도록 요청을 비활성화하고, 공식 단지 메타·가격 서버 상태·저장본을 분리. 재시도·연결 상태·실제 시세가 아닌 필터 동작 체험 추가.
- **부드러운 UI:** 세이지·아이보리 표면, 20px 카드·부드러운 그림자와 짧은 상태 전환을 지도·기록·실거래·연결 화면에 적용. 매매↔전세·면적별 즉시 재계산, 390px 전 화면 가로 넘침, 브라우저 오류 없음과 자동 테스트 35개 통과를 확인.

**다음:** 노출된 인증키 재발급, GitHub 재로그인·Actions Secret 등록, Firebase Blaze 전환 여부 결정 후 첫 공식 데이터 수집과 함수 배포·클라이언트 연결 활성화.

### 2026-06-12

**멀티 D-day 핀 기능 + 파일 구조 분리**

- **D-day 카드 멀티 표시:** 이벤트 행 클릭 시 D-day 카드가 교체되는 방식 → 카드가 상단에 추가되는 방식으로 전환. 2개, 3개 동시에 가로로 나열, 초과 시 가로 스크롤.
- **Firebase 핀 상태 저장:** 이벤트 문서에 `pinned: boolean` 필드 추가. `updateDoc`으로 토글. 새로고침 후에도 유지됨.
- **정렬:** 핀된 카드를 날짜 적게 남은 순 (미래 이벤트 우선, 동일 그룹 내 오름차순) 으로 정렬.
- **결혼식 중복 카드 버그 수정:** HTML 하드코딩 카드 + Firebase 핀 카드가 동시에 렌더되던 문제 해결. `renderDdayCards()`가 행 전체를 초기화 후 재렌더하도록 변경. 핀된 이벤트가 없을 때만 기본 결혼식 카드 표시.
- **파일 분리:** `index.html` 단일 파일에 있던 CSS/JS를 `css/style.css`, `js/app.js`, `js/firebase.js` 로 분리.

**다음:** wecost / honeymoon 각각 AGENTS.md 작성 완료.

### 2026-06-12 (honeymoon 집중 개발)

honeymoon 앱 대규모 UX 개선 — 상세 내용은 `honeymoon/AGENTS.md` 참조.

- 리조트 12개 / 여행사 3개로 확장 (투어민 추가)
- 핑크/로즈 테마로 전환 (sungso 메인과 통일)
- 5탭 UX 전면 개선: 카드 그리드↔분할 전환, 드래그 리사이즈, 지도 핀 직접 상세
- Firebase 연동 추가: `resort_notes` 서브컬렉션으로 성우/소희 댓글 메모 저장
- 메모 팝업: 상세 헤더 "💬 메모" 버튼 → 플로팅 채팅 UI
- 토너먼트: 매치 중 상세보기 버튼, 버그 수정
- sungso 홈으로 돌아가는 내비 바 추가

### 2026-06-13 (honeymoon 플랜 탭 + 편집 UX)

- **Detail Sheet 통일:** 모든 리조트 상세를 우측 슬라이드 패널(모바일: 바텀시트)로 단일화. `openDetailSheet` / `closeDetailSheet` API.
- **메모 UX 개선:** z-index 수정(5400), 메모 카운트 배지(카드/Pick슬롯/상세헤더), `resort_note_meta` 컬렉션 추가, 메모 알림 센터 드로어 추가
- **플랜 탭 (`tab-plan.js`):** 커플 Top3 Pick 슬롯, 최종 후보 + 확정, "🏆 우리의 리조트" 히어로 카드, 기본 일정 8일 자동 생성
- **일정표 편집 UX:** Day 카드 View/Edit 모드, 7종 항목 타입 시스템(항공/숙박/이동/식사/액티비티/휴식/메모), 자동저장(800ms debounce), ↑↓ 순서변경, 미정 pill, Firebase 콜백 억제
- **새 Firebase 컬렉션:** `couplePicks/main` (pick + 확정), `itineraries/main` (세부일정), `resort_note_meta/{id}` (댓글 수 캐시)

### 2026-06-13 (wecost 대규모 리팩토링 완료)

Google Sheets CSV → Firebase Firestore 전환 + UI 전면 개편 (단일 파일 → CSS/JS 모듈 분리).

- **파일 구조:** `index.html` 4탭 뼈대 + `css/` 6파일 + `js/` 8파일 (app.js, firebase.js, calc.js, utils.js, tab-*.js)
- **Firebase 마이그레이션:** 5개 컬렉션 `onSnapshot` 실시간 구독 (`wecost_settings`, `wecost_items`, `wecost_savings`, `wecost_loans`, `wecost_adjustments`)
- **4탭 UX:** 대시보드(결론 4카드+흐름바+일정+시뮬) / 현금흐름(저축 입력+누적 차트) / 결혼비용(요약+카테고리바+타임라인+테이블) / 집 시뮬레이션(가용현금+시나리오카드+대출 CRUD)
- **재무 색상 시스템:** `--income`(녹), `--expense`(적), `--warning`(황), `--house`(청록), `--loan`(보라) 의미 기반 토큰
- **즉시 반영:** 저축 입력 시 debounce 500ms → Firebase 저장, 동시에 로컬 computeAll() → 전 탭 재렌더

**다음:** GitHub Pages 배포 후 브라우저 검증.

### 2026-06-13 (wecost UX 개선 — "입력 → 계산 → 판단" 흐름 명확화)

- **대시보드 시작 체크리스트:** 6개 항목(결혼일/소희저축/성우저축/결혼비용/목표집가격/대출조건) 미완료 시 카드 표시. 각 항목에 바로가기 버튼. 전부 완료 시 자동 숨김.
- **메트릭 카드 빈 상태:** 저축 & 항목 0일 때 ₩0 대신 "계산 대기" + 원인 문구 표시.
- **현금흐름 계산식 시각화:** 스택 바 → [부부저축] + [지원금] + [조정] − [결혼비용] = [집에 쓸 수 있는 현금] 계산식 블록으로 교체. 각 블록에 탭 바로가기 버튼. 미니 흐름바는 보조로 유지.
- **결혼비용 항목 CRUD:** "+ 항목 추가" primary 버튼(상단 헤더) + 우측 슬라이드 드로어 UI. `addItem` / `updateItem` / `deleteItem` Firebase 연동 완료.
- **결혼비용 빈 상태:** 항목 0개일 때 empty state + "샘플 항목 불러오기" 버튼(5개 일괄 추가).
- **테이블 수정/삭제 버튼:** 각 행 우측에 수정(드로어 열림) / 삭제(confirm 후 deleteDoc) 버튼 추가.
- **집 탭 목표 집 가격 계산기:** 집 탭 최상단에 목표 집 가격 입력 → 가용현금 / 필요대출 / 월상환액 / 감당상태 즉시 표시. debounce 800ms → Firebase 저장.
- **현금흐름 탭 placeholder 개선:** 4개 저축 입력칸에 구체적 예시 값 추가.

### 2026-06-13~14 (honeymoon + 공통 편의 기능)

- **PIN 잠금:** sungso 루트 첫 진입 시 4자리 PIN(0306) 입력. localStorage 30일 토큰. wecost·honeymoon 직접 URL 접근도 PIN 게이트로 리다이렉트.
- **wecost UI 리디자인:** 라벤더 배경·보라 primary·둥근 카드(20px)·pill 버튼·캡슐 프로그레스바·카드 입장 애니메이션(stagger)·Chart.js 커스텀 툴팁.
- **새로고침 탭 복원:** honeymoon·wecost 모두 마지막 방문 탭을 localStorage에 저장 → 새로고침 시 동일 탭 복귀.
- **honeymoon 배지 겹침 수정:** 순위·Pick·블로그수 배지를 `.card-bl-badges` flex 컨테이너로 묶어 세로 적층.
- **honeymoon 상세 패널 Leaflet 지도:** 리조트 상세 "위치 & 이동" 아래 200px Leaflet 지도 추가. 리조트 핀(핑크) + 말레공항 핀(회색) + 점선 경로. "구글지도로 열기" 링크 포함.
- **지도에서 보기 버그 수정:** `closeCardDetail`(미존재) → `closeDetailSheet` 수정, Detail Sheet 닫힌 후 미니맵 정상 표시.
- **USD/KRW 환율 토글:** `firebase-fx.js` 신규. open.er-api.com 실시간 조회 → `honeymoon_fx/usd_krw` 저장. 탭 네비 우측 위젯(환율 라벨 + ⟳ 새로고침 + USD↔KRW 토글). 카드·가격비교·상세 가격 전환.
- **아나네아 가격 수정:** 허니문리조트 HB 기준 → HB+(AI 올인클루시브) 기준으로 교체. 비치풀$3,169 / 워터풀$3,322 / 믹스$3,246.

**다음:** 리조트 데이터 추가 보완, wecost 브라우저 검증.
