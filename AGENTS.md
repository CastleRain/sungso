# sungso — 프로젝트 루트

성우 & 소희의 결혼 준비 공간. GitHub Pages로 배포되는 정적 사이트 모음.

**배포 URL:** `https://CastleRain.github.io/sungso/`
**저장소:** `https://github.com/CastleRain/sungso` (branch: `master`)

---

## 폴더 구조

```
sungso/
├── index.html       ← 허브 랜딩 페이지 (이 문서의 대상)
├── css/
│   └── style.css    ← index.html 전용 스타일
├── js/
│   ├── app.js       ← 파티클, Flatpickr 초기화, D-day 초기 렌더
│   └── firebase.js  ← Firebase 모듈 (이벤트 CRUD, D-day 카드, 달력)
├── wecost/          ← 결혼 비용 관리 앱 (AGENTS.md 있음)
├── honeymoon/       ← 몰디브 신혼여행 플래너 (AGENTS.md 있음)
└── homehunt/        ← 방문 아파트·실거래 지도 포털 (AGENTS.md 있음)
```

---

## index.html 구성

빌드 도구 없음. 외부 의존성은 CDN으로만 로드. CSS/JS는 별도 파일로 분리.

### 외부 의존성
| 라이브러리 | 용도 | CDN |
|---|---|---|
| Firebase JS SDK v10.12.0 | Firestore 실시간 DB | `gstatic.com/firebasejs/10.12.0` |
| Flatpickr | 날짜 선택 달력 UI | `cdn.jsdelivr.net/npm/flatpickr` |
| Flatpickr 한국어 locale | 월/요일 한국어 표시 | `flatpickr/dist/l10n/ko.js` |

### 스크립트 구조

1. **`js/app.js`** (일반 스크립트) — Firebase 의존성 없는 코드
   - 파티클 생성 (하트/별 16개, CSS 애니메이션)
   - Flatpickr 초기화 (`#formDate`, 한국어, 오늘 이후만 선택)
   - 결혼식 D-day 초기 렌더 (Firebase 연결 전 로딩 상태용)

2. **`js/firebase.js`** (`type="module"`) — Firestore 연동 코드
   - Firebase 초기화 → Firestore `events` 컬렉션 `onSnapshot` 구독
   - `renderDdayCards()` — 핀된 이벤트를 날짜 적게 남은 순으로 카드 렌더
   - `renderList()` — 이벤트 리스트 렌더 (핀 토글, 삭제)
   - `renderCalendar()` — 달력 뷰 렌더
   - 이벤트 추가 (`addDoc`) / 삭제 (`deleteDoc`) / 핀 토글 (`updateDoc`)

### 주요 DOM ID
| ID | 역할 |
|---|---|
| `ddayRow` | D-day 카드들을 담는 flex 컨테이너 |
| `ddayNum` | 로딩 상태용 초기 D-day 숫자 (Firebase 연결 후 전체 교체됨) |
| `eventList` | 이벤트 리스트 렌더 컨테이너 |
| `calGrid` | 달력 그리드 렌더 컨테이너 |
| `calPopup` | 달력 날짜 클릭 시 팝업 |
| `addForm` | 일정 추가 폼 (토글) |
| `formTitle` / `formDate` | 추가 폼 입력 필드 |

### D-day 카드 동작
- 핀된 이벤트가 없으면: 결혼식 D-day 카드 1개 기본 표시
- 핀된 이벤트가 있으면: 핀된 이벤트만 카드로 표시 (날짜 적게 남은 순, 가로 스크롤)
- 이벤트 행 클릭 = 핀 토글 (행 전체 또는 📌 버튼); 핀 상태는 Firestore에 저장

---

## Firebase

**프로젝트:** `sungso-358cb`

```js
// js/firebase.js 에 하드코딩된 config
apiKey:            "AIzaSyBz-P5ycMAjYZBV7hkcZDrmq28EAw7Hsp8"
authDomain:        "sungso-358cb.firebaseapp.com"
projectId:         "sungso-358cb"
storageBucket:     "sungso-358cb.firebasestorage.app"
messagingSenderId: "143797950443"
appId:             "1:143797950443:web:95b0f616246d84aae3bae"
```

**Firestore 컬렉션**

`events` (sungso 루트 — 일정 관리):

| 필드 | 타입 | 예시 |
|---|---|---|
| `date` | string (YYYY-MM-DD) | `"2027-03-06"` |
| `title` | string | `"결혼식"` |
| `emoji` | string | `"💒"` |
| `pinned` | boolean | `true` (없으면 false 취급) |
| `createdAt` | serverTimestamp | — |

`resort_notes/{resortId}/comments` (honeymoon — 댓글 메모):

| 필드 | 타입 | 예시 |
|---|---|---|
| `author` | string | `"성우"` \| `"소희"` |
| `text` | string | `"라군뷰가 정말 예쁘다"` |
| `createdAt` | serverTimestamp | — |

**보안 규칙:** 현재 배포본은 `allow read, write: if true` (전체 공개). 저장소의 `firestore.rules`는 HomeHunt/서버 캐시를 차단하도록 준비됐지만, 기존 일정·재무·여행 컬렉션은 호환성을 위해 공개 상태를 유지한다. 전체 비공개화에는 Firebase Authentication이 필요하다.

**이벤트 추가/수정:** Firebase Console → Firestore → `events` 컬렉션에서 직접 편집하거나 페이지 UI를 사용.

**초기 시딩:** 컬렉션이 비어있을 때 페이지 첫 로드 시 자동으로 기본 이벤트 8개 삽입.

---

## 디자인 시스템

**색상**
- 메인 핑크: `#FF6B9D`
- 연한 핑크: `#FF85B3`, `#FFB3CC`, `#FFCCE0`, `#FFF0F5`
- 배경 그라디언트: `#FFF5F7` → `#FFF0EA`
- 텍스트: `#1a1a1a` (제목), `#333` (본문), `#aaa` (서브), `#ccc` (힌트)

**공통 패턴**
- 카드 border-radius: `22px` (프로젝트 카드), `16px` (이벤트 행), `28px` (D-day 카드)
- 그림자: `box-shadow: 0 2px 14px rgba(0,0,0,0.055)`
- hover: `translateY(-5px)` + shadow 강화 + 핑크 border

---

## 서브 프로젝트 (간략)

| 폴더 | URL | 데이터 소스 | 설명 |
|---|---|---|---|
| `wecost/` | `/sungso/wecost/` | Firebase Firestore | 결혼 비용·저축·집 계획 4탭 재무 대시보드 |
| `honeymoon/` | `/sungso/honeymoon/` | JS 하드코딩 + Firebase | 몰디브 12개 리조트 비교, 토너먼트, PDF 뷰어 |
| `homehunt/` | `/sungso/homehunt/` | localStorage + 국토부 API/JSON | 둘이 본 아파트 지도 기록, 면적별 실거래·추세 |

각 서브 프로젝트는 독립적인 `index.html` 보유. 세부 내용은 각 폴더의 `AGENTS.md` 참조.

**중요:** `wecost`는 Google Sheets CSV에서 **Firebase Firestore로 마이그레이션 완료**. `honeymoon`은 리조트 데이터는 JS 하드코딩이지만, **댓글 메모·커플 picks·일정표는 Firebase Firestore 사용** (세부 컬렉션은 `honeymoon/AGENTS.md` 참조).

---

## 작업 규칙
- 의미 있는 변경/완료 항목이 생기면, 매번 사용자에게 묻지 말고
  "진행 상황" 섹션을 알아서 업데이트할 것
- 큰 작업 끝나면 "다음에 뭐 할지" 도 한 줄 남겨둘 것

---

## 진행 상황

### 2026-09-06 — 공개 배포 구성 정리

- GitHub Pages가 `master / (root)`를 사용하고 저장소 배포 권한이 있음을 확인. 최신 원격 공급 데이터는 유지하고 HomeHunt·공용 계산·WeCost 집 탭 연결만 릴리스 체크아웃으로 분리.
- `docs/deployment.md`에 실제 Pages 배포 절차, 공개 화면/로컬 검색 서버의 기능 차이, 데이터 수집 Secrets와 전체 온라인 검색의 서버·인증 후속 순서를 기록.
- 개인 `.env`·로컬 캐시·신혼여행 수정·IDE 설정을 배포 대상에서 제외하고, 미수집 공개 실거래를 연결 완료로 표시하지 않도록 준비 상태를 보강.

**다음:** 공개 추천·통근 HTTPS API와 인증을 구현하고 공급원 Secrets를 연결해 휴대폰에서 전체 검색을 검증한다.

### 2026-09-06 (HomeHunt 4.2.1 지도 핀 통일)

- 단지 이름·조건 확인 전 표시와 분양 공고 이름을 추가하고 지역 숫자 핀과 동일한 흰 배경·둥근 외곽선으로 통일. 예전 가격 핀의 이중 배경·꼬리 제거.
- 넓은 지도 분양 이름 축약·확대/선택 시 펼침, 키보드 상세 열기와 핀 간 선택 잔상 해제 지원.
- 자동 테스트 361개, 데스크톱·390/430px 실화면·무가로넘침·브라우저 무오류 확인.

**다음:** 기존 통근 리뷰의 세 오류와 관심 지역 좌표 보강을 이어간다.

### 2026-09-06 (HomeHunt 4.2 지역 분포·입지 추천)

- 집 찾기의 기본 정렬을 회사 위치 없이 계산하는 강남·역 접근성 중심 입지 점수로 변경하고 항목별 추천 이유와 정렬 비중 전환 추가.
- 993곳을 36개 시군구의 숫자 지도·전체 지역 목록으로 나누고, 클릭 확대·지역 선택·후보 필터를 연결. 지역 대표점과 단지 위치를 구분하며 상위 40곳부터 확인하고 20곳씩 추가 보강.
- 국가철도공단 공식 역 좌표 1,090행과 강남역 기준점을 도입하고 직선거리·GTX 미수록·자료 기준일 한계 명시. 실제 지역 합계·필터·추가 위치 확인과 자동 테스트 355개와 데스크톱·390/430px 실화면·브라우저 무오류 확인.

**다음:** 기존 회사 통근 리뷰의 세 오류와 관심 지역 좌표 커버리지를 보강한다.

### 2026-09-06 (HomeHunt 슬라이더 조정)

- 집 찾기 슬라이더를 1,000세대·30년·50평·20억원·출근 120분까지로 줄이고 범위 양 끝값을 표시.
- 세대수·연식·면적·가격은 직접 입력한 큰 값을 조건·저장에 그대로 사용하도록 상한을 분리. 새로고침 유지·슬라이더 조작·모바일 화면과 자동 테스트 307개 확인, 사용자 기존 조건 복원.

**다음:** 통근 추천 리뷰의 우선 세 오류를 수정한다.

### 2026-09-06 (HomeHunt 4.1 총가격·평수별 비교)

- 실거래 그래프를 평균 총 거래가격 기준으로 전환하고 최근 거래월 평균과 선택기간 평균을 금액·거래건수와 함께 구분 표시.
- 모든 정확 전용면적의 평균가격·거래건수를 카드로 비교하고 클릭해 상세 흐름으로 이동하도록 구현. 전망 총액 표시와 실제·전망 월 정렬도 보강.
- 실제 단지 13개 면적·매매/전세·1/5년 전환, 데스크톱·390/430px 화면과 브라우저 무오류, 자동 테스트 307개를 검증.

**다음:** 통근 추천 리뷰의 우선 세 오류를 수정한다.

### 2026-09-06 (HomeHunt 조건 수정 접근성)

- 지역·세대수·연식을 기본 설정 상단으로 꺼내고 지도 조건 칩→해당 입력 바로가기를 연결. 모바일 `조건 변경` 표시·본문 스크롤·연식 0년 복원을 보강.
- 조건 변경·새로고침 유지와 390/430px 실제 화면, 자동 테스트 291개를 확인하고 화면 버전을 4.0.2로 갱신.

**다음:** 통근 추천 리뷰의 우선 세 오류를 수정한다.

### 2026-09-06 (HomeHunt 통근 추천 검토)

- 회사 위치 기반 추천을 읽기 전용 검토하고 관련 테스트 49개를 확인. 회사 수정 후 옛 기준 유지·주당 방문일 가중치 미갱신·대체 교통수단 실패 시 확정 제외 오류를 재현해 `homehunt/docs/commute-logic-review.md`에 기록.

**다음:** 세 우선 오류를 수정하고 조건 변경·부분 경로 실패의 통합 회귀 검증을 추가.

### 2026-09-06 (HomeHunt 4.0 리빌딩)

- 지도 탐색을 `지금 확인 / 가격 후보 / 지역 범위`로 재구성하고, 관심·방문·분양의 근거 상세와 3곳 판단 보드를 추가.
- 고정 72% 실거래 진행표시를 단계·경과시간·취소·재시도로 교체하고, 12개월 우선→3/5년 확장과 실패 시 자료 보존 구현.
- WeCost 계산을 공용 모듈로 분리하고 공개 금융 자동조회 없이 읽기 전용 연결 상태·개인 자금 시나리오를 추가.
- 분양 공급원 7상태·공식 신청 준비·포털 링크 전용 정책을 구현. 실제 5년 실거래 3,653건과 조건 후보 245개를 확인.
- 자동 테스트 291개와 390/430/1024/1440px 여섯 화면·실거래 필터 연동·후보 비교·자금 계산을 검증하고, `homehunt/docs/rebuild-implementation.md`에 실제 연결과 미연결 항목·실행/배포 설정을 정리.

**다음:** 운영 API 활용권한·Kakao 활성화와 Firebase Authentication 및 household 재무 권한을 연결.

### 2026-09-04 (HomeHunt 2.5 지도 중심 통합 탐색)

- `homehunt/docs/homehunt-2.5-handoff/`에 UI·화면, 데이터·로직, 다음 고도화 브리프와 실제 로컬 화면 캡처 14장을 묶어 후속 작업의 기준선을 기록하고, 내장 안내와 README의 예전 메뉴명을 현재 6개 정보구조로 통일.
- HomeHunt 메뉴를 `집 찾기 / 내 기록 / 실거래 / 분양·청약 / 사용 안내 / 연결 상태`로 재구성하고 모바일은 핵심 4메뉴와 상단 보조 진입을 유지.
- 집 찾기 지도에 실제 조건 후보 클러스터와 단지·분양·방문·관심·목적지 레이어를 추가하고, 방문 지도·목록·비교는 `내 기록` 내부로 통합.
- 후보·방문·분양에서 단지 실거래와 주변 지도까지 이어지는 교차 이동을 연결하고 분양 빠른 메뉴와 고밀도 화면을 적용.
- 검색 전 공식 단지는 가격 판정 전 기준점 12개로 제한하고 실제 검색 후 국토부 실거래 조건을 통과한 후보만 가격 결과로 표시.
- 화면·로컬 API 2.5.0 계약과 전체 자동 테스트 222개를 통과.

**다음:** 인수인계 브리프의 P0 순서대로 공식 분양 기준선, 단지 좌표 커버리지, 필터 경계 계약과 390px·430px 실화면을 검증.

### 2026-09-04 (HomeHunt 2.4 · 사용 안내·청약 준비도·휴대폰 알림)

- 처음 방문자가 목적지 등록→집 찾기→현장 기록→실거래·분양 확인으로 시작할 수 있는 독립 `사용 안내` 화면과 6개 기능 바로가기 추가.
- 서울·경기 분양/신혼희망타운을 동네·가격·평형·공급세대와 미공개 포함 여부로 거르는 브라우저 `내 조건`을 구현하고 알림 조건과 함께 로컬 저장.
- 성우·소희의 자가입력 민영 기본 84점과 신혼 공급 준비 항목을 브라우저에만 저장하며, 신혼특공·희망타운 공급비율을 당첨확률로 만들지 않고 공고일 원문 확인을 강제.
- 비공개 Telegram 그룹과 GitHub Actions를 연결해 닫힌 사이트에서도 신규·중요 변경을 받고, Repository Variables로 모바일 알림에도 지역·신혼·가격·평형·세대수 조건을 적용.
- baseline·stale·마감·삭제·비중요 변경 차단, 공식 링크 allowlist, 메시지 상한·429 재시도·해시 중복 억제를 적용하고 전체 자동 테스트 217개와 2.4.0 로컬 API·브라우저 무오류 검증 완료.

**다음:** 청약홈·LH 활용신청을 승인하고 Telegram Secrets/Variables를 설정해 실기기 알림을 검증한 뒤 Firebase Authentication·FCM 전환 여부를 결정.

### 2026-09-04 (HomeHunt 2.3 · 서울·경기 분양 알림)

- HomeHunt에 `분양 알림` 여섯 번째 화면을 추가하고 청약홈 APT·잔여·임의공급, LH 분양주택·신혼희망타운, SH 자체 분양 공고를 서울·경기로 통합.
- 검색·지역·접수상태·신혼/분양유형·관심 필터, 공고/접수/발표/계약 일정, ㎡·평 주택형·분양가·공급세대·원문 링크를 피드/상세 화면에 구현.
- 신혼희망타운 코드나 신혼부부 배정세대처럼 공식 구조화 근거가 있는 경우만 신혼 대상으로 확정하고 미제공 가격·일정·자격은 추측하지 않도록 처리.
- 공급원별 기준선·부분실패 보존·원자적 파일 갱신과 GitHub Actions 하루 3회 수집·Pages 명시 재빌드, 로컬 3시간 캐시·실패 후 15분 재시도 대기를 구축.
- 관심·읽음·알림 조건을 로컬 저장하고 신규·변경·D-3/D-1 브라우저 알림 및 캘린더 저장을 추가; 닫힌 브라우저 푸시는 Firebase 인증/FCM 후속 범위로 명시.
- SH 공식 RSS를 키 없이 수집해 EUC-KR 파싱·공급원별 기준선·부분 실패 보존을 적용하고, GH는 실시간 공식 피드가 없고 `robots.txt`가 자동 수집을 차단해 공고판·공식 문자알림을 보완 경로로 분리.
- 현재 공공데이터 키의 청약홈 401·LH 403을 실확인해 별도 활용신청 안내를 추가하고, 샘플·실제 미승인 상태와 SH 실응답을 검증한 뒤 전체 자동 테스트 189개·JS 문법 검사를 통과.
- 390px 정적 반응형 감사로 하단 6개 내비·필터·알림 모달의 무가로넘침을 확인하고 모바일 공고 선택 시 상세 카드로 자동 이동하도록 보강.

**다음:** 청약홈·LH 활용신청 승인 후 첫 공식 수집 기준선을 만들고, Firebase Authentication·Cloud Messaging으로 성우·소희 전용 백그라운드 푸시를 연결.

### 2026-09-04 (HomeHunt 2.2 지도 우선 탐색)

- 방문 지도와 맞춤 찾기를 네이버 부동산형 `전체 지도 + 한 줄 조건 칩 + 오버레이 드로어/결과 시트`로 전환해 검색·필터가 지도를 밀어내지 않도록 정리.
- 맞춤 지도를 검색 전부터 표시하고 조건 숫자 입력과 슬라이더를 동기화했으며, 결과 통계·후보 목록은 데스크톱 우측 패널/모바일 하단 시트로 분리.
- 추천 후보 묶음을 상태별 원형 숫자 클러스터로 표시하고 확대→세부 묶음→가격 핀→정확한 결과 카드 흐름을 연결.
- 2.2.0 로컬 서버에서 현재 저장 조건의 공식 가격·기본조건 후보 185개를 실제 조회하고, 데스크톱·390px 지도/패널·무가로넘침·브라우저 무오류와 자동 테스트 132개를 검증.

**다음:** 회사 A–D 실제 위치와 Kakao 선별을 연결해 제한된 TMAP 호출은 최종 후보 검증에만 사용하고, K-apt 역·주차·시설 정보를 결과 시트에 추가.

### 2026-09-04 (HomeHunt 2.1 · 평균가/검색 반응/화이트 UI)

- 사용자 화면의 실거래 대표값을 산술평균 거래가격과 평균 평당가격으로 통일하고 `1평≈3.3㎡` 의미·표본 수·최근 개별 거래를 함께 표시.
- 검색한 단지·거래유형·정확 전용면적·기간을 요약, 차트, 면적 비교, 최근 거래, 6개월 참고 예측에 하나의 데이터 문맥으로 연결.
- 검색 중 단계형 진행 상태와 스켈레톤을 추가하고, 긴 실거래 화면을 `단지 요약`·`가격 흐름`·`예측·근거` 하위 메뉴로 분리.
- 사이트 바탕을 흰색으로 바꾸고 중립 표면을 적용해 초록색은 주요 액션과 선택 상태에만 남김.
- 예전 저장 후보의 과거 대표값을 평균으로 잘못 표시하지 않고 재검색 갱신 대상으로 분리.
- 래미안안양메가트리아 실제 조회로 두 건의 산술평균, 매매↔전세·면적·1/3/5년 연동을 확인하고 자동 테스트 132개, 5개 화면 흰 배경, 데스크톱·390px 무가로넘침, 브라우저 무오류 통과.

**다음:** K-apt 상세·한국부동산원 지역 가격지수를 연결하고 인증된 Firebase에 두 사람의 방문 기록 공유를 이전.

### 2026-09-04 (HomeHunt 2.0 완료 · 다중 회사 균형 통근/안전 예측)

- 한 곳뿐이던 회사 위치를 최대 4개 목적지 A~D로 확장하고, 주당 방문일·교통수단·허용시간을 반영해 모든 필수 목적지가 확인된 후보만 통근 충족으로 판정.
- 직선거리는 후보 선별용으로만 남기고, 가장 불리한 통근을 우선 최소화한 뒤 가중 평균·도보·환승 부담을 보는 균형 점수와 목적지별 경로 행렬을 추가.
- 가격 후보 찾기와 경로 검증을 분리해 자동 TMAP 소진을 없애고, KST 일일 사용량·남은 쿼터·예상 호출 수를 확인한 뒤 상위 후보 또는 개별 후보만 정밀 검증하도록 변경.
- Kakao 대중교통 REST를 넓은 후보 선별에 쓰고 TMAP을 동일 출근시각의 최종 검증에 쓰는 공급자 선택, 배치 API와 일 10건 사전 차단을 로컬 서버에 구현.
- Kakao 1차 결과와 TMAP 최종 판정을 분리하고 Kakao·TMAP 모두 KST 일일 장부(기본 1,000/10회), 캐시 miss만 차감, 실행 전 전체 배치 한도 검사를 적용.
- 국토부 지역·월 응답이 재시도 뒤에도 불완전하면 해당 시군구 후보 전체를 추천에서 제외해 오래된 일부 거래를 `가격 검증됨`으로 오인하지 않도록 보강.
- 추천을 첫 화면으로 올리고 목적지 A~D 핀, 혼합 상태 클러스터, 예산 후보·통근 확인·최적 균형·최악 통근·남은 호출을 한 화면에서 판단하도록 UI를 재구성.
- 연결되지 않은 역 접근성은 하드 필터에서 제외하고, 방문 호가와 방문 당시·현재 동일면적 실거래 변화를 분리하며 현재 기준은 방문 후 거래만 사용하고 희소 표본은 낮은 신뢰도로 표시.
- 현재·직전 불완전월 제외, horizon별 rolling-origin, 무변화 기준 대비 5% 개선, MAE·MASE·경험적 참고범위로 예측 공개 조건을 강화.
- 단지 이력을 기본 5년으로 확장해 6개월 예측 검증 표본을 확보하고, 저장 후보의 목적지 조건 fingerprint 만료·재검증 동기화와 혼동 가능한 가짜 방문 핀 제거를 완료.
- 실제 `.env` 연결로 국토부 846건, NAVER 회사 검색·자동차 경로, TMAP 경로·무소모 한도 차단을 확인하고 자동 테스트 128개, 5개 화면·390px 무가로넘침·브라우저 무오류를 검증.
- TMAP 무료량 우회 금지, Kakao·TMAP 종량제 선택, 다중 통근·가격 구성·예측 한계를 공식 근거와 함께 `homehunt/docs/report-source.md`에 정리.

**다음:** `KAKAO_REST_API_KEY`를 로컬 환경에 연결해 실제 회사 3~4곳의 Kakao 선별→TMAP 최종 검증을 반복 확인하고, K-apt 역·주차·시설 보강 후 인증된 Firebase 저장소로 이전한다.

### 2026-09-04 (HomeHunt 1.8.0 · 로컬 키 자동 연결/모든 건물명 검색)

- 회사 위치 검색을 NAVER API HUB 지역 검색, NAVER Maps 주소, 키 없는 Kakao 공식 주소 DB 건물명 검색의 통합 UI로 확장.
- `KT`가 `정자KTe-편한세상` 아파트에 부분 일치하던 오인을 차단하고 회사 위치 검색에서 아파트 카탈로그 fallback을 제거.
- 장소 키가 없어도 등록 건물명·법인명·도로명·지번을 모달 안에서 선택한 뒤 NAVER 지도 좌표로 다시 확인하도록 구현.
- NAVER API HUB Client ID/Secret은 Git 제외 `homehunt/.env`로 재시작 후 자동 연결하거나 화면에서 현재 서버 메모리에만 연결하도록 분리하고, 공개 코드·Git·브라우저 저장소·응답에는 남기지 않도록 구성.
- 장소 검색 키 미연결·구형 서버·검색 실패를 구분해 건물 주소찾기, 입점 상호 API 연결, 지도 직접 선택을 유지.
- `KT` 공식 주소 후보 20페이지와 `푸르지오시티3차` 단일 건물 결과를 실제 브라우저에서 확인하고, 선택 주소가 NAVER 지도 좌표로 확정되는 흐름까지 검증.
- 우편번호 하위 모달의 비동기 세션 격리·포커스 이동/복귀·배경 `inert`·실패 시 지도 복구를 보강하고 390px/946px에서 가로 넘침과 브라우저 오류가 없음을 확인.
- `homehunt/.env` 자동 로딩, 공개 `.env.example`, 키별 공식 발급 가이드와 프로세스 환경변수 우선·보안 입력 fallback을 추가.
- 화면 입력은 계속 메모리 전용으로 유지하고 `.env`/환경변수 자동 연결 여부를 연결 상태에 구분 표시.
- 1.8.0 서버의 `.env` 국토부 키 자동 인식을 확인하고, 2026-08 안양시 만안구 매매·전월세 공식 API가 모두 HTTP 200·결과코드 000을 반환하는지 실연결 검증.
- `.env`의 NAVER API HUB·NAVER Directions 5·TMAP 키 실연결을 확인하고 `KT 분당본사` 5개 장소 후보, 테스트 경로의 자동차 23분·대중교통 44분 정상 응답을 검증.
- NAVER 지역 검색이 WGS84 좌표를 1천만 배 정수 문자열로 반환하는 실응답 형식을 정규화하고, 회사 결과 선택과 지도 핀까지 브라우저에서 확인.
- HomeHunt 1.8.0 공급자·검색·지도 회귀 자동 테스트 89개 통과.

**다음:** 필요한 NAVER Maps Secret·NAVER API HUB 지역 검색 키·TMAP appKey를 `homehunt/.env`에 연결해 실제 회사 검색·자동차·대중교통 경로를 검증하고, Firebase Function 배포를 이어간다.

### 2026-09-03 (HomeHunt 로컬 실거래·조건 추천)

- 서울·경기 17,851개 공식 아파트로 범위를 정리하고, 공개 코드·브라우저에 키를 남기지 않는 localhost 국토부 실거래 서버 구축.
- 자연어/직접 입력 조건에서 세대수·연식·전용면적·실거래 예산을 실제 판정하는 `맞춤 찾기` 화면 추가.
- 국토부 호출 속도 제한·재시도·오류 XML 검증·파일 캐시를 적용하고 204개 월·지역 요청이 누락 없이 완료되는지 검증.
- 관심 후보 저장·다시 보기, 지도·12개월 실거래 연결과 공식 카탈로그 기준일·미확인 교통 조건 표시를 보강.
- Tabler UI 1.4.0·Tabler Icons 3.46.0 기반 디자인 레이어를 추가해 5개 화면, 동적 카드, 폼, 모달, 모바일 내비를 부동산 서비스 톤으로 통일.
- 네이티브 폼·검색·저장 계약은 유지하고 접근성 상태, 터치 크기, 모바일 고정 액션과 반응형 레이아웃을 보강.
- 맞춤 찾기의 핵심 조건을 한 줄에 모으고 지도·후보 목록을 데스크톱 한 화면에 배치, 회사 핀·가격 핀·후보 수 클러스터를 추가.
- 회사 주소를 좌표로 확정한 뒤 전체 가격 후보를 거리순으로 재정렬하고, TMAP 대중교통·NAVER 자동차 실제 경로가 없으면 `1시간 충족`으로 판정하지 않도록 변경.
- 단지 이력을 최근 1·3·5년으로 확장하고 가격을 억·만원, 전용면적을 ㎡·평으로 통일.
- 추세 예측에 데이터 품질 게이트와 시간 순서 롤링 백테스트를 추가해 오차가 큰 결과는 보류하고 근거·한계를 화면과 조사 보고서에 표시.
- 구형 로컬 서버와 새 화면의 버전 차이를 감지해 잘린 추천 결과를 차단하고 재시작 안내를 제공.
- 이력의 서울 기준 시작·종료 월을 캐시 계약에 포함하고 부분 응답의 완전 캐시 덮어쓰기와 불완전 예측을 차단.
- 946px 핵심 조건 한 행, 결과 지도 자동 이동, 390px 전 화면 무가로넘침과 공용 모달 포커스 복귀를 검증하고 자동 테스트 75개 통과.
- 통근 조건 검색의 기본 결과·지도를 실제 경로상 시간 충족 후보로 한정하고, 미확인·초과 후보는 별도 결과 범위에서만 열어보도록 구분.
- 600개 초과의 넓은 가격 후보는 통근 미검증 상태로 기본 추천에 노출하지 않고 조건 축소를 안내하도록 차단.
- 새 검색 사전검사 실패 시 이전 조건의 결과·지도·상태를 즉시 비워 오래된 후보가 새 검색 결과처럼 보이지 않도록 보강.
- 회사 위치를 네이버 주소 일부 검색 후보 또는 전용 지도 클릭으로 좌표 확정할 수 있게 하고, 공식 주소가 입주 회사명과 다를 수 있음을 안내하며 검색·지도 응답 경합을 차단.
- 숫자 입력의 포인터 스피너와 포커스 중 휠 값 변경을 막아 작은 필터 칸의 실수 입력을 줄임.

### 2026-09-02 (HomeHunt 신규 구축)

- **부동산 포털 UI:** 네이버 지도 중심의 필터–지도–결과 3단 레이아웃, 방문 기록·실거래·연결 상태 화면 구축.
- **네이버 Maps:** Dynamic Map, 주소 검색·역지오코딩, 방문 마커, 지도 직접 선택 연동. Client Secret은 사용하지 않음.
- **방문 기록:** 매매·전세·월세, 면적·층·가격, 둘의 현장 의견·상태를 CRUD. 공개 Firestore 대신 로컬 저장 + JSON 백업·복원.
- **실거래 분석:** 국토부 매매·전월세를 5개 전용면적대로 분리, 중위 가격·3.3㎡ 단가·거래량·지역 순위·6개월 통계적 추세 예측 구현.
- **검색 캐시:** 트래킹 단지 정적 JSON, 브라우저 IndexedDB, Firebase Function의 월 원자료·단지 캐시, GitHub Actions 일일 지역 집계 구축.
- **전국·정확 검색:** 공식 법정동 269개 시군구 선택, 동명 단지 후보 분리, 실제 전용면적 0.1㎡별 이력, IndexedDB 재사용 구현.
- **공식 전국 카탈로그:** 한국부동산원 공동주택 45,920개를 정적 검색 목록으로 생성하고 지역 선택 없는 단지명·주소·동네 검색, `레미안 → 래미안` 보정, 유사 단지 추천 구현.
- **검색 표면 통일:** 지도·방문 기록·실거래·집 기록 폼을 자유 검색으로 통일하고 검색 위치 핀, 비동기 경합 차단, 모바일 필터 서랍과 공식 목록 연결 상태 추가.
- **수집 안정성:** 국토부 페이지네이션·월 원자료 캐시·부분 실패 대응·서버 서명과 호출 한도, Node 20 및 서버 컬렉션 차단 규칙 반영.
- **홈 연결:** sungso 랜딩에 HomeHunt 카드 추가, 모바일 하단 내비·JSON 데이터 파이프라인·자동 테스트 추가.
- **후보 결정 UX:** 최대 3곳 비교함, 확인 가격·면적·교통·연식·장단점·메모의 사실 기반 비교표와 로컬 선택 저장 추가.
- **방문→실거래 연결:** 방문 카드에서 공식 시군구·거래유형·가장 가까운 실제 전용면적을 자동 선택하고 현장 확인가와 최근 3건 중위 실거래 차이 표시.
- **가독성·검증:** 부동산 포털 정보 글자·모바일 비교 트레이를 개선하고 후보 비교·전국 단지 검색 순수 로직을 포함한 자동 테스트 33개 통과. 4개 화면과 390px 모바일 브라우저 검증 완료.
- **실연결 검증:** 네이버 지도 타일·방문 마커·주소 검색과 국토부 매매·전월세 API의 실제 XML 응답을 확인. 인증키 값은 저장소에 기록하지 않음.
- **단지 가격 상태 UX:** 미배포 Firebase Function의 404/CORS를 사용자 네트워크 오류로 표시하지 않도록 요청을 비활성화하고, 공식 단지 메타·가격 서버 상태·저장본을 분리. 재시도·연결 상태·실제 시세가 아닌 필터 동작 체험 추가.
- **부드러운 UI:** 세이지·아이보리 표면, 20px 카드·부드러운 그림자와 짧은 상태 전환을 지도·기록·실거래·연결 화면에 적용. 매매↔전세·면적별 즉시 재계산, 390px 전 화면 가로 넘침, 브라우저 오류 없음과 자동 테스트 35개 통과를 확인.

**다음:** 노출된 인증키 재발급, GitHub 재로그인·Actions Secret 등록, Firebase Blaze 전환 여부 결정 후 첫 공식 데이터 수집과 함수 배포·클라이언트 연결 활성화.

### 2026-06-12

**멀티 D-day 핀 기능 + 파일 구조 분리**

- **D-day 카드 멀티 표시:** 이벤트 행 클릭 시 D-day 카드가 교체되는 방식 → 카드가 상단에 추가되는 방식으로 전환. 2개, 3개 동시에 가로로 나열, 초과 시 가로 스크롤.
- **Firebase 핀 상태 저장:** 이벤트 문서에 `pinned: boolean` 필드 추가. `updateDoc`으로 토글. 새로고침 후에도 유지됨.
- **정렬:** 핀된 카드를 날짜 적게 남은 순 (미래 이벤트 우선, 동일 그룹 내 오름차순) 으로 정렬.
- **결혼식 중복 카드 버그 수정:** HTML 하드코딩 카드 + Firebase 핀 카드가 동시에 렌더되던 문제 해결. `renderDdayCards()`가 행 전체를 초기화 후 재렌더하도록 변경. 핀된 이벤트가 없을 때만 기본 결혼식 카드 표시.
- **파일 분리:** `index.html` 단일 파일에 있던 CSS/JS를 `css/style.css`, `js/app.js`, `js/firebase.js` 로 분리.

**다음:** wecost / honeymoon 각각 AGENTS.md 작성 완료.

### 2026-06-12 (honeymoon 집중 개발)

honeymoon 앱 대규모 UX 개선 — 상세 내용은 `honeymoon/AGENTS.md` 참조.

- 리조트 12개 / 여행사 3개로 확장 (투어민 추가)
- 핑크/로즈 테마로 전환 (sungso 메인과 통일)
- 5탭 UX 전면 개선: 카드 그리드↔분할 전환, 드래그 리사이즈, 지도 핀 직접 상세
- Firebase 연동 추가: `resort_notes` 서브컬렉션으로 성우/소희 댓글 메모 저장
- 메모 팝업: 상세 헤더 "💬 메모" 버튼 → 플로팅 채팅 UI
- 토너먼트: 매치 중 상세보기 버튼, 버그 수정
- sungso 홈으로 돌아가는 내비 바 추가

### 2026-06-13 (honeymoon 플랜 탭 + 편집 UX)

- **Detail Sheet 통일:** 모든 리조트 상세를 우측 슬라이드 패널(모바일: 바텀시트)로 단일화. `openDetailSheet` / `closeDetailSheet` API.
- **메모 UX 개선:** z-index 수정(5400), 메모 카운트 배지(카드/Pick슬롯/상세헤더), `resort_note_meta` 컬렉션 추가, 메모 알림 센터 드로어 추가
- **플랜 탭 (`tab-plan.js`):** 커플 Top3 Pick 슬롯, 최종 후보 + 확정, "🏆 우리의 리조트" 히어로 카드, 기본 일정 8일 자동 생성
- **일정표 편집 UX:** Day 카드 View/Edit 모드, 7종 항목 타입 시스템(항공/숙박/이동/식사/액티비티/휴식/메모), 자동저장(800ms debounce), ↑↓ 순서변경, 미정 pill, Firebase 콜백 억제
- **새 Firebase 컬렉션:** `couplePicks/main` (pick + 확정), `itineraries/main` (세부일정), `resort_note_meta/{id}` (댓글 수 캐시)

### 2026-06-13 (wecost 대규모 리팩토링 완료)

Google Sheets CSV → Firebase Firestore 전환 + UI 전면 개편 (단일 파일 → CSS/JS 모듈 분리).

- **파일 구조:** `index.html` 4탭 뼈대 + `css/` 6파일 + `js/` 8파일 (app.js, firebase.js, calc.js, utils.js, tab-*.js)
- **Firebase 마이그레이션:** 5개 컬렉션 `onSnapshot` 실시간 구독 (`wecost_settings`, `wecost_items`, `wecost_savings`, `wecost_loans`, `wecost_adjustments`)
- **4탭 UX:** 대시보드(결론 4카드+흐름바+일정+시뮬) / 현금흐름(저축 입력+누적 차트) / 결혼비용(요약+카테고리바+타임라인+테이블) / 집 시뮬레이션(가용현금+시나리오카드+대출 CRUD)
- **재무 색상 시스템:** `--income`(녹), `--expense`(적), `--warning`(황), `--house`(청록), `--loan`(보라) 의미 기반 토큰
- **즉시 반영:** 저축 입력 시 debounce 500ms → Firebase 저장, 동시에 로컬 computeAll() → 전 탭 재렌더

**다음:** GitHub Pages 배포 후 브라우저 검증.

### 2026-06-13 (wecost UX 개선 — "입력 → 계산 → 판단" 흐름 명확화)

- **대시보드 시작 체크리스트:** 6개 항목(결혼일/소희저축/성우저축/결혼비용/목표집가격/대출조건) 미완료 시 카드 표시. 각 항목에 바로가기 버튼. 전부 완료 시 자동 숨김.
- **메트릭 카드 빈 상태:** 저축 & 항목 0일 때 ₩0 대신 "계산 대기" + 원인 문구 표시.
- **현금흐름 계산식 시각화:** 스택 바 → [부부저축] + [지원금] + [조정] − [결혼비용] = [집에 쓸 수 있는 현금] 계산식 블록으로 교체. 각 블록에 탭 바로가기 버튼. 미니 흐름바는 보조로 유지.
- **결혼비용 항목 CRUD:** "+ 항목 추가" primary 버튼(상단 헤더) + 우측 슬라이드 드로어 UI. `addItem` / `updateItem` / `deleteItem` Firebase 연동 완료.
- **결혼비용 빈 상태:** 항목 0개일 때 empty state + "샘플 항목 불러오기" 버튼(5개 일괄 추가).
- **테이블 수정/삭제 버튼:** 각 행 우측에 수정(드로어 열림) / 삭제(confirm 후 deleteDoc) 버튼 추가.
- **집 탭 목표 집 가격 계산기:** 집 탭 최상단에 목표 집 가격 입력 → 가용현금 / 필요대출 / 월상환액 / 감당상태 즉시 표시. debounce 800ms → Firebase 저장.
- **현금흐름 탭 placeholder 개선:** 4개 저축 입력칸에 구체적 예시 값 추가.

### 2026-06-13~14 (honeymoon + 공통 편의 기능)

- **PIN 잠금:** sungso 루트 첫 진입 시 4자리 PIN(0306) 입력. localStorage 30일 토큰. wecost·honeymoon 직접 URL 접근도 PIN 게이트로 리다이렉트.
- **wecost UI 리디자인:** 라벤더 배경·보라 primary·둥근 카드(20px)·pill 버튼·캡슐 프로그레스바·카드 입장 애니메이션(stagger)·Chart.js 커스텀 툴팁.
- **새로고침 탭 복원:** honeymoon·wecost 모두 마지막 방문 탭을 localStorage에 저장 → 새로고침 시 동일 탭 복귀.
- **honeymoon 배지 겹침 수정:** 순위·Pick·블로그수 배지를 `.card-bl-badges` flex 컨테이너로 묶어 세로 적층.
- **honeymoon 상세 패널 Leaflet 지도:** 리조트 상세 "위치 & 이동" 아래 200px Leaflet 지도 추가. 리조트 핀(핑크) + 말레공항 핀(회색) + 점선 경로. "구글지도로 열기" 링크 포함.
- **지도에서 보기 버그 수정:** `closeCardDetail`(미존재) → `closeDetailSheet` 수정, Detail Sheet 닫힌 후 미니맵 정상 표시.
- **USD/KRW 환율 토글:** `firebase-fx.js` 신규. open.er-api.com 실시간 조회 → `honeymoon_fx/usd_krw` 저장. 탭 네비 우측 위젯(환율 라벨 + ⟳ 새로고침 + USD↔KRW 토글). 카드·가격비교·상세 가격 전환.
- **아나네아 가격 수정:** 허니문리조트 HB 기준 → HB+(AI 올인클루시브) 기준으로 교체. 비치풀$3,169 / 워터풀$3,322 / 믹스$3,246.

**다음:** 리조트 데이터 추가 보완, wecost 브라우저 검증.
