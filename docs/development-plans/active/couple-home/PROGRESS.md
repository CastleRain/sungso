# 진행·검증 기록

## 2026-09-13 — 시작과 사용자 변경 지시

- 기준: 최신 `master`의 계획 인계 `620c1fc`. 12종 청첩장 `4014d00`, 여행 예산·출발 준비 `eb8bea8`이 포함된 구현을 확인했다.
- 기존 작업 폴더의 미커밋 변경을 유지하고 `codex/couple-home` 작업 공간에서 개발한다. 저장소 전체 재배치는 하지 않는다.
- 루트 README·AGENTS·architecture와 Hub·Invitation·WeCost·Travel·Honeymoon·HomeHunt 및 서비스 지침, PLAN·CHECKLIST를 읽고 프로토타입을 브라우저에서 확인했다.
- 사용자가 지정한 두 Google 계정은 기존 Firebase Authentication에 존재하고 모두 Google 공급자·검증된 이메일을 갖춘 것을 관리자 읽기로 확인했다. 회원 문서는 아직 등록하지 않았다. 이메일·UID·토큰은 공개 기록에 포함하지 않는다.
- **범위 변경:** 사용자가 PDF는 GitHub에 그대로 두고 제한 Drive 공유는 나중에 설정한다고 지시했다. PDF 10개와 현재 뷰어·공개 경로를 유지한다. 이 파일들은 인증으로 보호되지 않으며 Drive 이전·공개 파일 제거 조건은 미완료다. 다른 구현이 끝나도 해당 조건을 완료로 간주하거나 `completed`로 이동하지 않는다.
- 홈·날짜·공유 메모, 공통 인증·앱 연결, 규칙·서버 인증, 정적 개인 자료 이전 준비를 병행한다. 실제 이전은 백업·dry-run·재실행 시 기존 값 보존 검증 뒤에만 수행한다.

## 2026-09-13 — 회원 등록·기준 자료 이전

- 관리자 읽기로 15개 컬렉션 67개 문서를 비공개 백업했다. dry-run에서 기존 재무 2개 문서를 보존하고 기준 자료 2개·회원 2개만 생성하도록 검토했다. 모르는 서버 컬렉션 7개는 별도로 확인하고 변경하지 않았다.
- 06:56:51 UTC 이전 결과: 새 문서 4개·기존 문서 보존 2개·생성 후 검증 4개. 이전 전 운영 문서 67개 전체의 fields 해시가 동일하며 변경 0개다. QA 테스트 데이터는 운영에 쓰지 않았다.
- 백업 SHA-256: `05cb588b5289b9c3fd4462541f21d6f2745743916254724afb90d056eea88560`; dry-run: `4dfa83e3a33a44fe8b94fe84010b907faa0479b6b7fbe8c7f3a65b48698abced`.
- 원문·개인 식별자·운영 백업은 Git 제외 `.private-migration/`에 보관한다. 공개 기록에는 건수·해시만 남긴다. PDF 10개(4,922,926 bytes)는 변경하지 않았다.

## 로컬 구현·검증 중간 결과

- 밝은 공유 홈·날짜 분리·메모, 공통 Google 인증·명명 앱 동기화, 최신 청첩장/Travel–WeCost 연결, 정적 기준 이전을 구현했다. 1차 전체 check 1,495개 통과 후 추가 lifecycle 검토를 진행한다.
- 별도 보안 검증: Emulator 36개·관련 Node 47개·서비스 번들 3개 통과. [보안 근거](SECURITY-VALIDATION.md)와 [개인 자료 감사](PRIVATE-DATA.md)를 참고한다.
- 브라우저 대역에서 비회원 차단·두 회원 홈 동기화·편집 취소·충돌 초안 유지·전체 숨김/복원·메모 일반 텍스트·3+6 더보기·기존 날짜 고정/달력을 확인했다. 나머지 앱·모바일과 운영 배포는 검증 중이다.
- 운영 Functions 목록은 0개다. 새 Functions/Blaze를 추가하지 않고 기존 Render 배포를 사용한다.

다음: 구독/늦은 응답 검토를 마치고 최종 검사·7개 앱 1440/390px QA·규칙/Pages/Render 배포를 확인한다. 외부 키 교체·실제 회원 로그인·PDF 제한 공유 미완료는 active 상태로 명시한다.

## 2026-09-13 — 최종 구현·배포 검증

- 늦은 구독/저장 응답·SDK 초기화/계정 전환을 추가 보강한 최종 `npm run check` 1,519개·참조 365개·문법 137개, 서비스 번들 3개가 통과했다. Emulator 36개도 통과했다. 7개 앱의 1440/390px 주요 화면과 대역 실패/충돌을 확인했다.
- `1935118`을 최신 master에 fast-forward push했다. [Pages 34745590531](https://github.com/CastleRain/sungso/actions/runs/34745590531) 성공, Render 같은 소스 `dep-daj549ek1f9s73fhfia0` Live. 회원 규칙 배포·소스 해시 일치와 익명 개인 컬렉션 11종 403, 인증 API 401을 확인했다.
- 공개 자산 218/218 일치·PDF 10개 byte 보존·개인 호환 경로 7개 404. 07:40 UTC 운영 원본 67개 재대조에서 변경/누락 0개다.
- 실제 성우 Google 세션으로 홈·날짜·WeCost·Travel·청첩장 읽기와 직접 링크를 확인했다. 실제 로그아웃 뒤 개인 내용 제거를 확인했고 공개 홈은 다시 로그인할 수 있는 상태다. 소희의 실제 로그인은 사용자 입력이 필요하다.
- 초기 CLI 계정 조회 출력의 토큰 노출을 확인해, 자동 검토의 광범위 폐기 거절 후 실제 현재 계정 1개로 범위를 확인·제한했다. 07:44 UTC 해당 CLI 인증만 원격 폐기했으며 남은 CLI 계정 0개다. 서버 키와 Firebase 웹 인증 설정을 변경하지 않았다.
- [RESULTS](RESULTS.md)와 [OPERATIONS](OPERATIONS.md)에 변경·검증·커밋·배포·한계를 정리했다. 전체 완료가 아니므로 `completed`로 이동하지 않는다.

다음: 사용자가 공개 홈에서 Google 재로그인하고 소희 계정도 확인한다. NAVER Developers의 기존 노출 검색 키 폐기/재발급·Render 설정, 이후 PDF 제한 Drive 공유를 처리한다. 다음 Firebase 관리자 작업 전 CLI에 다시 로그인한다. 비밀값은 채팅이나 문서로 받지 않는다.

## 2026-09-13 — 후속 요청: 청첩장 시그니처 세 가지

사용자가 기존 예시 중 약 세 가지를 더 고급스럽고 풍부한 체험으로 확장하도록 요청했다. 기존 12개 ID를 유지하고 봉투·별자리·탑승권의 전체 본문을 시그니처 에디션으로 보강한다. 기존 초안/공동 선택과 Google 인증을 유지하며 실제 하객 응답이나 운영 QA 쓰기는 추가하지 않는다.

다음: 세 본문과 조작을 1440px/390px 대역에서 확인하고 필수 검사·Pages 배포 근거를 기록한다. 원래 계획의 NAVER 키·소희 로그인·PDF 보호 후속은 그대로 미완료다.

세 본문과 목록 소개를 구현했다. 루트 검사 1,542개·Invitation 51개·Emulator 18개 통과, 기존 9종 HTML 27개와 이전 12종 옵션/초안 보존을 확인했다. 1440px/390px 대역에서 체험·모바일 옵션·초안/충돌·로그아웃을 검증했으며 운영 DB 쓰기·공급자 호출은 없다. 다음은 구현 커밋의 Pages 배포와 정적 자산 대조다.

`6d8667f` master 반영과 [Pages 34747581063](https://github.com/CastleRain/sungso/actions/runs/34747581063)의 검사·서비스 빌드·배포 성공을 확인했다. 공개 청첩장 자산28/28과 로컬 빌드 해시가 일치했다. 이번 청첩장 추가 요청은 구현·배포까지 완료했으며, 전체 홈 계획은 기존 외부 키·실회원 로그인·PDF 보호 후속 때문에 active에 유지한다. 다음은 두 사람이 예시를 비교하고 실제 제작 범위를 정하는 것이다.

## 2026-09-13 — 완료 감사와 추가 보강

- 현재 파일의 과거 작업 기록도 개인정보 보호 범위에 포함해 문서 8개·프로토타입 HTML 2개의 실제 여행/예식 날짜·시각·견적을 일반화했다. 원본 텍스트 10개와 시안 PNG 3개를 비공개 백업·해시 검증했고, 실제 날짜가 보이지 않는 PNG는 보존했다. 현재 파일 정리와 과거 Git 이력 재작성은 구분한다.
- 로그아웃 SDK 거절 시 개인 화면과 구독을 즉시 정리하고 실패 상태를 유지하도록 보강했다. 별도 비식별 잠금으로 새로고침·다른 앱·다른 탭의 회원 자동 복원을 차단하고 명시적 재시도를 제공한다. 초안 키는 삭제하지 않는다. 저장소 제한과 오래된 탭 이벤트, HomeHunt 내부 로그아웃도 함께 검토했다.
- 1440px/390px 대역에서 실패·다른 탭 차단·새로고침·청첩장 직접 링크 잠금·재시도 후 정상 로그인/같은 미리보기 복구를 확인했다. 대역 쓰기·운영 요청·브라우저 오류·가로 넘침은 0이다. 새 오류 화면은 evidence의 `logout-failed-*`에 보관했다.
- 실제 접근 검증의 남은 범위를 정정했다. 소희 전체 앱과 성우 Honeymoon/HomeHunt가 미확인이며, Honeymoon은 자동 환율 쓰기를 차단한 읽기 전용 검증이 필요하다. 현재 연결 가능한 브라우저의 공개 홈과 NAVER Developers는 로그인이 필요하다. 두 로그인 화면을 사용자 패널에 준비하고 입력을 요청했다.

다음: 보강된 최종 검사를 마치고 commit·push·Pages 자산을 대조한다. NAVER 소유자 로그인/키 교체·남은 실회원 검증·사용자가 보류한 PDF 보호는 계속 미완료다.

최종 전체 검사 1,556개·인증/HomeHunt 대상 48개와 내부 로그아웃 대역 검증을 통과했다. 보강 커밋 `ea578cb411ad004b6a980d28a7c024c3fcf36a30`을 최신 master에 fast-forward push했고 원래 작업 폴더의 다른 미커밋 변경은 그대로다. [Pages 34748753391](https://github.com/CastleRain/sungso/actions/runs/34748753391)은 현재 GitHub 대기열에 있으며 배포 성공으로 처리하지 않았다. 다음은 실행 완료와 공개 자산 대조다.

추가 배포 확인 당시: 8분 이상 `queued`/작업 0개인 실행의 취소는 이미 완료된 실행이라는 응답으로 거절됐지만, 캐시를 우회한 조회는 계속 대기였다. 공개 대조(09:09:03 UTC)도 변경 JS 4개의 미반영을 확인했다. 같은 master로 한 번 수동 재시도했으나 GitHub API HTTP 500으로 실패했다. 이 시점에는 보강 배포를 미완료로 남겼으며, 아래 후속 성공 실행과 자산 대조로 공개 반영을 확인했다.

## 2026-09-14 — 재개 후 추가 보강 배포 차단 해소

- 작업 재개 시 `6e7c0c7` 뒤의 자동 공공 `home-supply.json` 갱신 1개를 fast-forward로 보존했다. 확인한 HEAD는 `c11ab3a1912d9d66a8a5e12813eb5f7110ac9521`이다.
- 이 커밋의 `workflow_dispatch` 실행 [Pages 34761005184](https://github.com/CastleRain/sungso/actions/runs/34761005184)가 성공했다. 2026-09-13 13:52:08 UTC validate에서 루트 check·서비스 번들 3개가 통과했고 13:52:20 UTC deploy가 성공했다.
- 같은 소스를 빌드한 뒤 2026-09-13 14:52:36.736 UTC 공개 파일 11개를 대조했다. 모두 HTTP 200이며 LF로 정규화한 SHA-256이 일치한다. 이전에 미반영이었던 공통 인증 모듈 2개·HomeHunt JS 2개도 포함한다. 원본 근거는 Git 제외 `.private-migration/completion-public-evidence.json`이다.
- `34748753391`을 포함한 기존 대기 실행 2개는 재개 후에도 `queued`다. 해당 실행을 성공으로 바꾸지 않고 후속 실행의 성공으로 배포 차단이 해소된 것으로 기록한다.

다음: NAVER 신규 키 재발급·Render 설정과 이전 키 폐기를 각각 확인하고, 남은 실제 회원 QA를 진행한다. 사용자 지시로 GitHub에 유지한 PDF 보호도 보류 상태이며 계획은 `active`에 유지한다.

## 2026-09-14 — NAVER 완료 조건 분리·실제 세션 확인

- NAVER 공개 브라우저 키·공개 프록시 제거와 회원 전용 Render 서버 경계는 구현 완료다. 이 시점에는 신규 키 재발급/Render 설정과 노출된 이전 키 폐기를 별도 미완료 조건으로 CHECKLIST·RESULTS에 나눠 기록했다. 후속 설정 완료는 아래에 기록한다.
- 소유자 로그인에서 대상 ‘성우소희’ 애플리케이션과 Render의 기존 지역 검색 키가 같은 애플리케이션의 값임을 확인했다. 새 키 적용에는 `HOMEHUNT_PROVIDER_CONFIG`의 `NAVER_LOCAL_SEARCH_CLIENT_SECRET` 교체와 `NAVER_SEARCH_CLIENT_ID`·`NAVER_SEARCH_CLIENT_SECRET` 추가가 필요하고 다른 공급자 설정은 유지하기로 했다. 이 시점에는 재발급 확인이 미완료였으며 서버 환경설정을 저장하지 않았다. 실제 값은 기록하지 않는다.
- [NAVER 공식 공지](https://developers.naver.com/notice/article/33626)는 2026-08-26 이후 재발급된 경우 이전 Client Secret도 30일간 유효하다고 안내한다. 즉시 삭제는 별도 문의가 필요하므로 재발급·교체만으로 폐기 완료라고 기록하지 않는다.
- 성우의 홈 로그인 복구와 HomeHunt 회원 화면 진입을 확인했다. 처음에는 숨겨진 시간 초과 안내 문구를 서버 장애로 읽었으나, 아래 실제 연결 패널 확인에서 오탐으로 정정했다. 성우 Honeymoon과 소희 전체 앱의 실제 검증은 남아 있다.

다음: 신규 키 재발급·Render 설정을 완료한 뒤 이전 키 폐기 근거를 별도로 확보하고, HomeHunt 서버 연결과 남은 회원 접근을 확인한다. PDF 보호 보류와 미완료 조건 때문에 `active`를 유지한다.

## 2026-09-14 — 신규 키 설정 완료·HomeHunt 확인 정정

- 사용자의 재발급 뒤 실제 애플리케이션의 Client ID 동일·Secret 변경을 확인했다. 기존 Render `HOMEHUNT_PROVIDER_CONFIG`를 비공개 백업·검증하고 지역 검색 Secret을 교체했으며 블로그 검색 ID/Secret을 추가했다. 총 11개 필드 중 다른 기존 8개는 유지했다. 2026-09-13 15:16:47.553 UTC 재조회에서 신규 키 일치를 확인했고 실제 값은 기록하지 않았다.
- 소스 `c11ab3a`의 Render `dep-dajbrqvqj5pc73d0p2g0`가 Deploy succeeded·Live·Free임을 확인했다. `/healthz` 200·익명 `/api/health` 401이며, 요청 장부 쓰기가 있는 블로그 API는 QA로 호출하지 않았다. `.private-migration/naver-rotation-evidence.json`이 비공개 근거다. 이전 키 폐기는 재발급/설정과 별도 미완료 조건이다.
- 2026-09-13 15:19:54.685 UTC 실제 HomeHunt 연결 패널은 온라인 서버 정상·공식 단지 17,851개·계정 전용 저장을 표시했고 표시 중인 경고는 0개였다. 앞서 시간 초과로 기록한 문구는 숨겨진 안내여서 오탐으로 정정한다. 성우의 실제 회원 진입·회원 API 접근을 확인했으며 `.private-migration/real-homehunt-read-evidence.json`에 근거를 보관한다.
- 개인 저장·백업 복원 버튼과 통근 실행은 누르지 않았다. 자동 진입의 `/recommendations/recent` 요청 제한 장부와 저장 후보의 `/kapt/complex` 캐시는 쓰기가 발생할 수 있다. 화면의 저장 후보는 0개였으나 자동 요청·장부 쓰기 발생 여부는 미측정이다. 이번 실제 확인을 과거 대역의 운영 DB 쓰기 0회 근거에 포함하지 않고, 개인 변경·훼손이나 백업 복원 검증으로 추정하지 않는다. 이후 전체 실제 HomeHunt QA에는 자동 요청 차단 대역이 필요하다.

문서 반영 검증: 최종 문서 11개에서 회원 이메일·이전/신규 NAVER Secret 일치 0건, 로컬 링크 63개 누락 0건과 `git diff --check` 통과를 확인했다. `check-site`는 공개 파일 228개의 내부 참조 382개를 통과했다. 최신 master `c11ab3a`와 원래 작업 폴더의 무관한 미커밋 변경 4개 보존을 확인했으며 이 기록은 추가 운영 API 검증을 뜻하지 않는다.

다음: 아직 유효할 수 있는 이전 NAVER 키의 즉시 폐기를 [미발송 문의 초안과 완료 근거](NAVER-KEY-REVOCATION.md)에 따라 별도로 확인하고 성우 Honeymoon·소희 전체 앱의 실제 접근을 검증한다. 필요한 로그인과 자동 쓰기 차단 준비를 기다리며, 사용자 보류 PDF와 남은 조건 때문에 `active`를 유지한다.

## 2026-09-14 — 재개 기록 push·Pages 성공

기록 11개를 `5ebc25e`로 커밋해 최신 master에 fast-forward push했다. Pages `34765783215`의 validate·deploy가 각각 2026-09-13 15:30:41 UTC·15:30:54 UTC에 성공했다. 15:32:19.755 UTC 공개 11개 파일이 HTTP 200·LF 정규화 SHA-256으로 모두 일치했다. 원래 작업 공간의 다른 미커밋 변경 4개는 그대로다. 성공한 실행과 비공개 근거 위치를 RESULTS에 추가했으며, 남은 조건과 active 상태는 유지한다.
