# 원본 구성 청첩장 6종 검증

2026-09-14. 공식 샘플의 [구성 대응과 차이](invitation-reference-research.md)를 기준으로 살롱드레터 3종·투아워게스트 3종을 추가했다. 신규 ID와 기존 23종·기존 저장 키·회원 검증·본인 찜·공동 선택 revision을 보존한다.

## 구현 범위

- 목록에 원본 샘플 구성 6개 묶음과 검색·비교·모바일 보기·직접 링크를 연결했다. 전체 29종이다. 별도 상단 스크롤 소개 선반은 되살리지 않았다.
- 새 6종은 긴 문서 스크롤에 최초 등장만 적용한다. 섹션 등장과 표지 글씨 시작을 따로 기억해 글자가 화면에 들어올 때 재생한다. 위로 복귀하거나 팔레트를 바꾸고 같은 탭에서 다시 열어도 이미 시작한 인트로는 재시작하지 않는다. 새로고침은 새로운 미리보기다.
- 갤러리는 native 가로 스크롤·이전/다음·카운터·사진 확대를 지원한다. 색상·너비 변경 때 보고 있던 사진을 복원한다. 기존 세 갤러리 옵션과 섹션 숨김을 유지한다.
- 종이 극장 본문은 사진 카드, 집 본문은 이미 열린 방, 리본 본문은 사진 타임라인으로 바꿔 표지와 같은 펼침을 다시 보여주지 않는다. 기존 표지의 스크롤 순역재생은 유지한다.
- 실제 사진·연락처·계좌·참석 응답을 수집하지 않는다. 원본 상용 서체·사진·소스 코드를 포함하지 않는다. 자동 RSVP 팝업과 상거래는 옮기지 않았다.

## 자동 검사

동시 작업의 테마·다른 앱 변경을 제외한 `/private/tmp/sungso-reference-release`에서 기준 `bd77a5a8`와 이번 변경만 검증했다.

| 검사 | 결과 |
|---|---|
| `npm run build` | 10개 앱·공개 파일 282개 |
| `npm test` | 1,659개 통과 |
| `npm run check` | 1,659개·공개 참조 458개·JS 문법 177개 통과 |
| 새 렌더러 검사 | 원본별 3개·6개 직접 링크·고유 등장 키·필수/옵션·갤러리·기존 초안 보존 |
| 새 flow 검사 9개 | 최초/재진입·인트로 실제 가시성·옵션 전환·키보드·감소/꺼짐·오래된 콜백·위치/갤러리 복원 |
| Firestore Emulator | 26개 통과: 신규 6종·최대 29개 찜/공동 선택, 초과·익명/비회원·상대방 수정 거부 |

Firestore 운영 변경은 `validFavorites`의 허용 ID 6개와 최대 길이 23→29뿐이다. 기존 회원·본인·revision 조건은 바꾸지 않았다. 원본 구성을 문서/테스트에서 단순 복제하는 검사보다 옵션·읽기·재생 생명주기를 확인한다.

## 브라우저 확인

운영 연결을 차단하는 `tests/site/couple-qa-server.mjs`를 8877(통합 작업 트리)과 8879(분리 릴리스)에서 사용했다.

- 390×844: 새 6개의 서로 다른 표지와 긴 본문·달력·갤러리·안내·마무리, 가로 넘침/깨진 이미지 없음. 원본의 대략적인 비율과 배치 비교. 섹션은 아래로 읽을 때 등장하고 위로 복귀해도 다시 숨지 않는다.
- 손글씨: 최초 화면 밖에서 `animation:none`, `opacity:0`; 글자가 보이면 `ref-intro-play` 및 필기 시작. 색상 변경 후 `ref-intro-seen`, `animation:none`, `opacity:1`로 완성 상태 유지.
- 포르투 갤러리: 2/3 사진을 보고 색상 변경 후 2/3과 가로 위치 유지, 사진 확대도 2/3으로 열림. 실제 응답·사진 업로드는 실행하지 않았다.
- 1440×900: 29개 목록·원본 6개 필터, 각 원본 링크, 390/430px 모바일 프레임. 360×640에서도 글씨·표지·일반 본문 흐름을 확인했다.
- 기본/모바일 보기, 연출 끄기/다시 켜기 및 본문 조작을 확인했다. 모션 감소·키보드 포커스는 자동 검사에서도 검증했다.

테스트 대역의 저장·트랜잭션·운영 요청·공급자 요청은 0이었다. 별도 Firestore Emulator 쓰기는 로컬 검사 데이터다.

## 한계

실제 iPhone/Android, Safari·다른 모바일 브라우저, OS 모션 감소 전환의 실기기 체감은 미검증이다. 원본 효과의 정확한 밀리초와 글자 획 단위 경로를 계측하지 않았다. 같은 사진·서체를 쓰는 픽셀 동일 복제는 아니다. 본문 정보와 갤러리 사진 수·유효 기능은 현재 예시 앱의 범위를 따른다. 운영 두 회원의 실제 저장 QA는 실행하지 않았다.

## 배포

운영 규칙 사전 조회(13:12:51Z)는 기존 Git 규칙과 일치했다. 원격 반영 결과는 배포 확인 후 아래에 추가한다. 로컬 통과만으로 배포 완료로 처리하지 않는다.

- 규칙 릴리스: 2026-09-14 13:16:46Z. 13:19:18Z 운영/로컬 SHA-256 일치 `8db9a8c111525e96460fc3ee8051feeb1787125a653a2b1896ae1106d5708365`. Functions·요금제·운영 문서는 변경하지 않았다.
